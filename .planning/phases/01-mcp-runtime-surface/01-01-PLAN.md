---
phase: 01-mcp-runtime-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - rust-toolchain.toml
  - crates/executor-core/Cargo.toml
  - crates/executor-core/src/lib.rs
  - crates/executor-core/src/error.rs
  - crates/executor-core/src/schema/mod.rs
  - crates/executor-core/src/schema/strategy.rs
  - crates/executor-core/src/schema/action.rs
  - crates/executor-core/src/schema/execution.rs
  - crates/executor-core/src/schema/policy.rs
  - crates/executor-core/src/schema/prompt_args.rs
  - crates/executor-core/tests/schema_snapshots.rs
  - crates/executor-core/tests/schemas/.gitkeep
  - crates/executor-state/Cargo.toml
  - crates/executor-state/src/lib.rs
  - crates/executor-signer/Cargo.toml
  - crates/executor-signer/src/lib.rs
  - crates/executor-mcp/Cargo.toml
  - crates/executor-mcp/src/lib.rs
  - crates/executor-mcp/src/main.rs
  - crates/executor-mcp/tests/common/mod.rs
  - crates/executor-mcp/tests/stdio_handshake.rs
autonomous: true
requirements:
  - MCP-01
must_haves:
  truths:
    - "Workspace는 executor-mcp, executor-core, executor-state, executor-signer 네 crate만 포함하고 cargo check로 빌드된다"
    - "executor-mcp crate에서 println!/eprintln!/dbg! 사용 시 clippy가 -D warnings로 차단한다"
    - "executor-core는 tool/prompt 입력 스키마 구조체를 JsonSchema derive와 함께 노출한다"
    - "Wave 0 harness 파일(stdio_handshake.rs, common/mod.rs, schema_snapshots.rs)이 존재하여 후속 plan이 테스트 본문만 채우면 된다"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace root — 4 crate member 선언 + workspace.dependencies + workspace.lints.clippy"
      contains: "members = [\"crates/executor-mcp\", \"crates/executor-core\", \"crates/executor-state\", \"crates/executor-signer\"]"
    - path: "rust-toolchain.toml"
      provides: "Stable 툴체인 핀 (2024 edition crate들이 사용)"
      contains: "channel = \"stable\""
    - path: "crates/executor-core/src/schema/strategy.rs"
      provides: "StrategyRegisterInput, StrategyIdInput, StrategyRunOnceInput 스키마 구조체"
      contains: "derive(.*JsonSchema.*)"
    - path: "crates/executor-core/src/schema/policy.rs"
      provides: "PolicyUpdateInput 스키마 구조체"
      contains: "struct PolicyUpdateInput"
    - path: "crates/executor-core/src/schema/execution.rs"
      provides: "ExecutionIdInput 스키마 구조체"
      contains: "struct ExecutionIdInput"
    - path: "crates/executor-core/src/schema/prompt_args.rs"
      provides: "WriteEvmStrategyArgs, ReviewEvmStrategyArgs prompt arg 구조체"
      contains: "struct WriteEvmStrategyArgs"
    - path: "crates/executor-core/tests/schema_snapshots.rs"
      provides: "JsonSchema golden snapshot 테스트 (UPDATE_SCHEMAS env로 golden 갱신)"
      contains: "schema_for!"
    - path: "crates/executor-mcp/tests/common/mod.rs"
      provides: "spawn_server / send / recv helper (line-delimited JSON-RPC)"
      contains: "async fn spawn_server"
    - path: "crates/executor-mcp/tests/stdio_handshake.rs"
      provides: "Integration test 진입 파일 (Plan 02/03이 테스트 함수를 추가)"
      contains: "mod common"
  key_links:
    - from: "Cargo.toml"
      to: "crates/executor-mcp/Cargo.toml"
      via: "workspace member + workspace.dependencies inherit"
      pattern: "rmcp.workspace = true"
    - from: "crates/executor-mcp/Cargo.toml"
      to: "crates/executor-core"
      via: "path dependency"
      pattern: "executor-core = \\{ path = \"../executor-core\" \\}"
    - from: "crates/executor-core/src/schema/strategy.rs"
      to: "schemars::JsonSchema"
      via: "derive"
      pattern: "#\\[derive\\(.*JsonSchema.*\\)\\]"
---

<objective>
Rust workspace skeleton과 Phase 1에서 필요한 네 crate(`executor-mcp`, `executor-core`, `executor-state`, `executor-signer`)를 생성한다. 도메인 스키마 구조체(Plan 02가 tool/prompt 라우터에 바인딩할 타입)를 `executor-core`에 미리 확정하고, Nyquist Wave 0 요구사항인 테스트 harness 파일 scaffold를 생성한다.

Purpose: Plan 02/03이 `cargo test -p executor-mcp --test stdio_handshake <name>` 명령을 바로 실행할 수 있도록 workspace/crate/test-harness 기반을 완성한다.
Output: 빌드 가능한 4-crate workspace, JsonSchema derive가 붙은 input 구조체, Wave 0 harness 파일들.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mcp-runtime-surface/01-CONTEXT.md
@.planning/phases/01-mcp-runtime-surface/01-RESEARCH.md
@.planning/phases/01-mcp-runtime-surface/01-VALIDATION.md
@AGENTS.md

<interfaces>
<!-- 이 plan이 **새로 생성**하는 계약. Plan 02/03은 여기서 선언된 타입명을 그대로 import 한다. -->

executor-core::schema::strategy::
  - `StrategyRegisterInput { name: String, source: String, metadata: Option<serde_json::Value> }`
  - `StrategyIdInput { strategy_id: String }`
  - `StrategyRunOnceInput { strategy_id: String }`

executor-core::schema::execution::
  - `ExecutionIdInput { execution_id: String }`
  - (`SignedTransaction` placeholder — executor-signer가 참조; 필드는 Phase 6에서 확정, 지금은 `pub struct SignedTransaction;` 유닛 struct)

executor-core::schema::policy::
  - `PolicyUpdateInput { /* metadata: Option<serde_json::Value> */ }` — Phase 5가 채울 placeholder 필드 + JsonSchema derive

executor-core::schema::prompt_args::
  - `WriteEvmStrategyArgs { intent: String, chain_hint: Option<String> }`
  - `ReviewEvmStrategyArgs { strategy_id: String }`

executor-signer::Signer (trait, 메서드 없음 — boundary만)
executor-state:: (module doc comment만)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace root + 4 crate skeleton + stdout-discipline lint 구성</name>
  <read_first>
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Cargo Workspace Layout" (전체 디렉토리 트리, Cargo.toml shapes, workspace.lints.clippy 블록)
    - .planning/phases/01-mcp-runtime-surface/01-CONTEXT.md D-01, D-01a, D-01b, D-05, D-05a
    - AGENTS.md "Hard Boundaries" 섹션 (stdio MCP must not write logs to stdout)
  </read_first>
  <files>
    - Cargo.toml
    - rust-toolchain.toml
    - crates/executor-core/Cargo.toml
    - crates/executor-core/src/lib.rs
    - crates/executor-state/Cargo.toml
    - crates/executor-state/src/lib.rs
    - crates/executor-signer/Cargo.toml
    - crates/executor-signer/src/lib.rs
    - crates/executor-mcp/Cargo.toml
    - crates/executor-mcp/src/lib.rs
    - crates/executor-mcp/src/main.rs
  </files>
  <action>
    루트 `Cargo.toml`을 RESEARCH.md §"Root Cargo.toml" 블록 그대로 작성한다 (문자 그대로):
      - `[workspace] resolver = "2"`, `members = ["crates/executor-mcp", "crates/executor-core", "crates/executor-state", "crates/executor-signer"]` (D-01 준수: 정확히 4개 member, strategy-js/executor-evm/executor-policy는 절대 추가하지 않는다).
      - `[workspace.package] edition = "2024"`, `version = "0.1.0"`, `license = "Apache-2.0"`.
      - `[workspace.dependencies]`: `rmcp = { version = "1.5", features = ["server", "macros", "schemars", "transport-io"] }`, `schemars = "1.2"`, `serde = { version = "1", features = ["derive"] }`, `serde_json = "1"`, `tokio = { version = "1", features = ["macros", "rt-multi-thread", "io-std", "signal"] }`, `tracing = "0.1"`, `tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "std"] }`, `anyhow = "1"`, `thiserror = "2"`, `toml = "0.8"`.
      - `[workspace.lints.clippy]` 블록에 `print_stdout = "deny"`, `print_stderr = "deny"`, `dbg_macro = "deny"` (D-05 #1 — stdout discipline).

    `rust-toolchain.toml`: RESEARCH.md §"rust-toolchain.toml" 블록 그대로:
      ```
      [toolchain]
      channel = "stable"
      components = ["rustfmt", "clippy"]
      profile = "minimal"
      ```

    각 crate `Cargo.toml`은 RESEARCH.md §"crates/executor-{mcp,core}/Cargo.toml" 문자열을 그대로 반영:
      - `executor-core`: deps = `schemars.workspace, serde.workspace, serde_json.workspace, thiserror.workspace`. **`rmcp` 의존 금지** (core는 순수 도메인 crate).
      - `executor-state`: `[package]` + deps = `executor-core = { path = "../executor-core" }` 만. Phase 1에서는 빈 module doc만 둔다.
      - `executor-signer`: `[package]` + deps = `executor-core = { path = "../executor-core" }` 만. `Signer` trait 정의만.
      - `executor-mcp`: RESEARCH.md §"crates/executor-mcp/Cargo.toml" 블록 그대로 (`[[bin]] name = "executor-mcp" path = "src/main.rs"` 포함). `[dev-dependencies]`에 `tokio = { workspace = true, features = ["macros", "rt-multi-thread", "io-std", "signal", "process", "io-util", "time"] }`, `serde_json.workspace = true`, `anyhow.workspace = true`.

    각 crate 진입 파일 내용:
      - `crates/executor-mcp/src/lib.rs` 최상단: `#![deny(clippy::print_stdout, clippy::print_stderr, clippy::dbg_macro)]` (D-05 #1 crate-level 이중방어). 그 아래 `pub fn placeholder() {}` 하나만 두어 Plan 02가 module 추가할 여지 남긴다.
      - `crates/executor-mcp/src/main.rs`: Phase 1 Plan 01 시점에서는 서버를 아직 와이어링하지 않는다. 다음 stub을 둔다:
        ```rust
        #![deny(clippy::print_stdout, clippy::print_stderr, clippy::dbg_macro)]
        fn main() {
            // executor-mcp bin entry — 실제 stdio serve는 Plan 02가 구현.
        }
        ```
      - `crates/executor-core/src/lib.rs`: `pub mod error; pub mod schema;` 선언 + `#![deny(clippy::print_stdout, clippy::print_stderr, clippy::dbg_macro)]`.
      - `crates/executor-state/src/lib.rs`: `//! Strategy/execution/journal persistence boundary — Phase 2에서 SQLite 구현.` (doc comment 한 줄만).
      - `crates/executor-signer/src/lib.rs`: `//! Signer boundary — Phase 6에서 local signer 구현.` + `use executor_core::schema::execution::SignedTransaction;` + `pub trait Signer: Send + Sync {}` (메서드 없음, 경계만).

    `cargo check --workspace`가 성공해야 한다. 실패 시 상단 파일만 수정하고 `Cargo.lock`은 자동 생성되게 둔다.
  </action>
  <verify>
    <automated>cargo check --workspace 2>&amp;1 | tee /tmp/plan-01-01-check.log &amp;&amp; grep -q "Finished" /tmp/plan-01-01-check.log &amp;&amp; cargo clippy -p executor-mcp -- -D warnings 2>&amp;1 | grep -qE "(Finished|warning:.*0 warnings)"</automated>
  </verify>
  <done>
    - `cargo check --workspace` 성공 (0 errors, 0 warnings)
    - `cargo clippy -p executor-mcp -- -D warnings` 성공
    - `grep -c "members" Cargo.toml` = 1 이고 해당 라인에 정확히 4개 crate만 나열
    - `grep "print_stdout" Cargo.toml` 매치 (D-05 workspace lint 확인)
    - `grep "#!\[deny(clippy::print_stdout" crates/executor-mcp/src/lib.rs` 매치
    - `grep "#!\[deny(clippy::print_stdout" crates/executor-mcp/src/main.rs` 매치
    - `rust-toolchain.toml` 존재 + `channel = "stable"` 포함
  </done>
</task>

<task type="auto">
  <name>Task 2: executor-core에 tool/prompt 입력 스키마 구조체 정의 + 에러 타입</name>
  <read_first>
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"rmcp 1.5 API Reference" — `StrategyRegisterInput`, `WriteEvmStrategyArgs`, `ReviewEvmStrategyArgs` 샘플
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Code Examples" — `executor-core/src/schema/strategy.rs` 예시
    - .planning/phases/01-mcp-runtime-surface/01-CONTEXT.md D-02, D-02c, D-03, D-04
    - .planning/REQUIREMENTS.md MCP-02, MCP-03, MCP-04 설명
    - crates/executor-core/src/lib.rs (Task 1에서 생성)
  </read_first>
  <files>
    - crates/executor-core/src/schema/mod.rs
    - crates/executor-core/src/schema/strategy.rs
    - crates/executor-core/src/schema/action.rs
    - crates/executor-core/src/schema/execution.rs
    - crates/executor-core/src/schema/policy.rs
    - crates/executor-core/src/schema/prompt_args.rs
    - crates/executor-core/src/error.rs
  </files>
  <action>
    `schema/mod.rs`: `pub mod strategy; pub mod action; pub mod execution; pub mod policy; pub mod prompt_args;` 한 줄씩.

    `schema/strategy.rs` — `#[derive(Debug, Clone, serde::Serialize, serde::Deserialize, schemars::JsonSchema)]`로 세 구조체 정의:
      - `StrategyRegisterInput { name: String, source: String, #[serde(default, skip_serializing_if = "Option::is_none")] metadata: Option<serde_json::Value> }`. 각 필드에 `#[schemars(description = "...")]` (RESEARCH.md 블록 그대로).
      - `StrategyIdInput { strategy_id: String }`.
      - `StrategyRunOnceInput { strategy_id: String }`.
      - 상단 `#[schemars(description = "...")]` 컨테이너 description 포함 (D-02b의 "placeholder임을 description에 명시" 준수 — 예: "Register a JavaScript strategy (Phase 2 implements persistence).").

    `schema/execution.rs`:
      - `ExecutionIdInput { execution_id: String }` (JsonSchema derive 동일).
      - `pub struct SignedTransaction;` — 유닛 struct placeholder. Phase 6에서 실제 필드 채움. 주석 필수: `/// Placeholder. Phase 6에서 rlp 인코딩된 tx payload 필드 추가.`

    `schema/action.rs`:
      - `pub enum Action` placeholder. Variants만 이름으로 선언, 본문 `;`:
        ```rust
        #[derive(Debug, Clone, serde::Serialize, serde::Deserialize, schemars::JsonSchema)]
        #[serde(tag = "kind", rename_all = "snake_case")]
        pub enum Action {
            // Phase 4에서 실제 variant가 채워짐. Phase 1은 enum 껍데기만.
            Noop,
        }
        ```
      - `// TODO(phase-4): ContractCall, RawCall, Erc20Approve, Erc20Transfer, NativeTransfer variants` 주석.

    `schema/policy.rs`:
      - `PolicyUpdateInput { #[serde(default, skip_serializing_if = "Option::is_none")] metadata: Option<serde_json::Value> }` (Phase 5에서 실제 필드로 교체; Phase 1은 빈 shape + JsonSchema derive).
      - `#[schemars(description = "Replace current policy (Phase 5 fills out the real shape).")]`.

    `schema/prompt_args.rs`:
      - `WriteEvmStrategyArgs { intent: String, #[serde(default, skip_serializing_if = "Option::is_none")] chain_hint: Option<String> }`.
      - `ReviewEvmStrategyArgs { strategy_id: String }`.
      - 두 struct 모두 `#[derive(Debug, Clone, serde::Serialize, serde::Deserialize, schemars::JsonSchema)]` + 컨테이너 `#[schemars(description = "...")]`.

    `error.rs`:
      - `use thiserror::Error;` + `pub enum CoreError { #[error("invalid input: {0}")] InvalidInput(String) }` — Phase 2+가 variant 추가. Phase 1은 한 variant만.

    모든 파일 상단에 `//! ...` doc comment 한 줄씩.

    D-05 준수를 위해 core crate 루트에도 `#![deny(clippy::print_stdout, clippy::print_stderr, clippy::dbg_macro)]` 적용 (Task 1에서 이미 `lib.rs`에 있는지 확인, 없으면 추가).
  </action>
  <verify>
    <automated>cargo check -p executor-core 2>&amp;1 | grep -q "Finished" &amp;&amp; cargo clippy -p executor-core -- -D warnings 2>&amp;1 | grep -qE "(Finished|warning:.*0 warnings)" &amp;&amp; grep -q "StrategyRegisterInput" crates/executor-core/src/schema/strategy.rs &amp;&amp; grep -q "WriteEvmStrategyArgs" crates/executor-core/src/schema/prompt_args.rs &amp;&amp; grep -q "PolicyUpdateInput" crates/executor-core/src/schema/policy.rs &amp;&amp; grep -q "ExecutionIdInput" crates/executor-core/src/schema/execution.rs</automated>
  </verify>
  <done>
    - `cargo check -p executor-core` 성공
    - `cargo clippy -p executor-core -- -D warnings` 성공
    - 6개 스키마 구조체 모두 존재 및 `JsonSchema` derive 포함 (grep 확인)
    - `executor-core`는 `rmcp`에 의존하지 않는다 (`grep rmcp crates/executor-core/Cargo.toml` 결과 없음 — core crate 경계 유지)
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 테스트 harness + schema golden snapshot 테스트 scaffold</name>
  <read_first>
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Integration Test Harness Shape" (spawn_server / send / recv helper 코드)
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Schema Contract Stability Test" (schema_snapshots.rs 샘플 + UPDATE_SCHEMAS 패턴)
    - .planning/phases/01-mcp-runtime-surface/01-VALIDATION.md §"Wave 0 Requirements" 체크리스트 (파일 경로 정확히 일치)
    - .planning/phases/01-mcp-runtime-surface/01-VALIDATION.md §"Per-Task Verification Map" (Plan 02/03 테스트 함수 이름)
    - crates/executor-core/src/schema/* (Task 2 산출물)
  </read_first>
  <files>
    - crates/executor-core/tests/schema_snapshots.rs
    - crates/executor-core/tests/schemas/.gitkeep
    - crates/executor-mcp/tests/common/mod.rs
    - crates/executor-mcp/tests/stdio_handshake.rs
  </files>
  <action>
    **1. `crates/executor-core/tests/schema_snapshots.rs`** — RESEARCH.md §"Schema Contract Stability Test" 패턴 적용:
      - `use schemars::schema_for;` + `use executor_core::schema::{strategy::*, execution::*, policy::*, prompt_args::*};`.
      - `fn assert_schema_matches_golden(name: &str, schema: impl serde::Serialize)` — `UPDATE_SCHEMAS` env가 있으면 `tests/schemas/<name>.json`에 pretty-print 저장, 없으면 기존 파일과 비교. 파일이 없고 `UPDATE_SCHEMAS`도 없으면 `panic!("missing golden file: tests/schemas/{name}.json. Run UPDATE_SCHEMAS=1 cargo test to create.")`.
      - 테스트 함수 6개 (각 input struct당 하나):
        ```
        #[test] fn strategy_register_input_schema_stable() { ... schema_for!(StrategyRegisterInput) ... }
        #[test] fn strategy_id_input_schema_stable() { ... schema_for!(StrategyIdInput) ... }
        #[test] fn strategy_run_once_input_schema_stable() { ... schema_for!(StrategyRunOnceInput) ... }
        #[test] fn execution_id_input_schema_stable() { ... schema_for!(ExecutionIdInput) ... }
        #[test] fn policy_update_input_schema_stable() { ... schema_for!(PolicyUpdateInput) ... }
        #[test] fn write_evm_strategy_args_schema_stable() { ... schema_for!(WriteEvmStrategyArgs) ... }
        #[test] fn review_evm_strategy_args_schema_stable() { ... schema_for!(ReviewEvmStrategyArgs) ... }
        ```
      - `tests/schemas/.gitkeep` 빈 파일 생성. 실제 golden JSON은 Plan 02가 `UPDATE_SCHEMAS=1 cargo test -p executor-core` 1회 실행으로 채운다.
      - `Cargo.toml` `[dev-dependencies]`에 `schemars.workspace = true`, `serde.workspace = true`, `serde_json.workspace = true` 추가 (이미 [dependencies]에 있어도 dev scope 명시 필요 없음 — 단, `serde_json`은 확인).

    **2. `crates/executor-mcp/tests/common/mod.rs`** — RESEARCH.md §"Integration Test Harness Shape" 블록 그대로 복사 (약 50줄):
      - `pub struct ServerProc { pub child: Child, pub stdin: ChildStdin, pub stdout: BufReader<ChildStdout> }`
      - `pub async fn spawn_server() -> anyhow::Result<ServerProc>` — `Command::new(env!("CARGO_BIN_EXE_executor-mcp"))`, `.env("RUST_LOG", "error")`, `.stdin/stdout/stderr(Stdio::piped())`, `.kill_on_drop(true)`, stderr drain task.
      - `pub async fn send(proc: &mut ServerProc, msg: serde_json::Value) -> anyhow::Result<()>`
      - `pub async fn recv(proc: &mut ServerProc) -> anyhow::Result<serde_json::Value>` — 핵심 assertion: `serde_json::from_str` 성공 + `jsonrpc == "2.0"` 필드.
      - `pub async fn initialize(proc: &mut ServerProc) -> anyhow::Result<serde_json::Value>` — `protocolVersion: "2025-11-25"`, `notifications/initialized` follow-up.
      - 상단 `#![allow(dead_code)]` — Plan 02/03가 사용하기 전까지 unused 경고 억제.

    **3. `crates/executor-mcp/tests/stdio_handshake.rs`** — 진입 파일:
      ```rust
      //! Phase 1 integration tests — 각 Plan이 함수를 추가한다.
      //! - Plan 02: tools_list_emits_full_surface, unimplemented_tools_return_phase_hint, readonly_tools_return_placeholder
      //! - Plan 03: resources_surface_matches_contract, prompts_surface_matches_contract, stdout_is_strict_jsonrpc, schema_contract_round_trip

      mod common;

      // Plan 02/03이 이 파일에 #[tokio::test] 함수를 추가한다.
      // Wave 0 단계: 파일 존재만 확인 — 실제 테스트는 후속 plan이 작성.

      #[tokio::test]
      async fn harness_compiles() -> anyhow::Result<()> {
          // Plan 01 확인용: common module이 컴파일되고 bin이 spawn 가능한지만 확인.
          let _ = common::spawn_server().await?;
          Ok(())
      }
      ```

    `crates/executor-mcp/Cargo.toml` `[dev-dependencies]`가 이미 Task 1에서 `tokio` process/io-util/time feature + `serde_json.workspace` + `anyhow.workspace`을 포함하는지 확인; 부족하면 보완.

    `cargo test --workspace --no-run`이 컴파일만이라도 통과해야 한다 (실제 테스트 실행은 Plan 02의 server 구현 후 가능; Plan 01의 `harness_compiles` 테스트는 bin이 즉시 종료하는 stub이라 spawn 직후 child가 죽어도 `spawn_server`는 성공 반환하므로 통과 — child는 stdin 파이프가 연결된 채로 main()이 Ok(())로 나가면서 종료).
  </action>
  <verify>
    <automated>cargo test --workspace --no-run 2>&amp;1 | grep -qE "(Finished|Compiling)" &amp;&amp; test -f crates/executor-mcp/tests/common/mod.rs &amp;&amp; test -f crates/executor-mcp/tests/stdio_handshake.rs &amp;&amp; test -f crates/executor-core/tests/schema_snapshots.rs &amp;&amp; grep -q "spawn_server" crates/executor-mcp/tests/common/mod.rs &amp;&amp; grep -q "schema_for!" crates/executor-core/tests/schema_snapshots.rs</automated>
  </verify>
  <done>
    - `cargo test --workspace --no-run` 성공 (harness가 컴파일됨)
    - VALIDATION.md Wave 0 Requirements 중 `crates/executor-mcp/tests/stdio_handshake.rs`, `crates/executor-mcp/tests/common/mod.rs`, `crates/executor-core/tests/schema_snapshots.rs` 세 파일 존재
    - `spawn_server`, `send`, `recv`, `initialize` 네 함수가 common/mod.rs에 pub로 존재
    - schema_snapshots.rs가 7개 input struct에 대한 `#[test]` 함수를 선언
    - Plan 02가 `cargo test -p executor-mcp --test stdio_handshake <fn_name>`으로 바로 테스트 함수만 추가 가능한 상태
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → runtime | Workspace clippy lints 및 crate-level `#![deny(...)]` 없이는 `println!`/`eprintln!`/`dbg!` 코드가 stdio 프로토콜을 파괴할 수 있다 |
| dev-dependencies → prod binary | `tokio::process` 같은 dev-only feature가 실수로 prod dep에 섞이면 공격 표면 증가 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | stdout stream | mitigate | Workspace `[workspace.lints.clippy]`의 `print_stdout/print_stderr/dbg_macro = "deny"` + crate-level `#![deny(...)]`로 컴파일 타임 차단 (D-05 #1) |
| T-01-01-02 | Information Disclosure | tracing logs → stdout | mitigate | `tracing-subscriber`는 Plan 02에서 `with_writer(std::io::stderr)`로 초기화 예정. Plan 01은 lint 방어막만 심어둠 |
| T-01-01-03 | Tampering | schemars derive drift | mitigate | `tests/schema_snapshots.rs` golden file로 스키마 변경을 의식적 행위로 강제 (PITFALLS.md Pitfall 3) |
| T-01-01-04 | Elevation of Privilege | signer trait 노출 범위 | accept | Phase 1은 empty `Signer` trait (메서드 없음) — 공격 표면 0. Phase 6에서 실제 메서드 추가 시 재평가 |

</threat_model>

<verification>
- `cargo check --workspace` 성공 (Task 1)
- `cargo clippy --workspace -- -D warnings` 성공 — workspace clippy lints가 실제로 작동하는지 확인 (Task 1–2)
- `cargo test --workspace --no-run` 성공 — 테스트 harness가 컴파일됨 (Task 3)
- `grep -c "members" Cargo.toml` = 1 (단일 workspace root)
- `grep -r "print_stdout" Cargo.toml crates/executor-mcp/src/ crates/executor-core/src/ | wc -l` >= 3 (workspace + 2 crate-level deny)
- `ls crates/ | wc -l` = 4 (D-01: 정확히 4 crate)
</verification>

<success_criteria>
- Workspace가 `resolver = "2"`와 `edition = "2024"`로 설정되어 `cargo check --workspace` 성공
- 네 crate(`executor-mcp`, `executor-core`, `executor-state`, `executor-signer`)의 `Cargo.toml` + `src/lib.rs`(또는 `main.rs`) 존재
- `executor-core`가 `rmcp` 의존성 없이 JsonSchema derive 구조체 7개를 노출
- Clippy print_stdout/print_stderr/dbg_macro 규칙이 workspace + crate-level 이중으로 걸려 있음
- Wave 0 harness 파일(`stdio_handshake.rs`, `common/mod.rs`, `schema_snapshots.rs`) 존재 — Plan 02/03이 테스트 함수만 추가하면 되는 상태
- `cargo test --workspace --no-run` 성공 (harness 컴파일)
</success_criteria>

<output>
완료 후 `.planning/phases/01-mcp-runtime-surface/01-01-SUMMARY.md` 생성. 포함 내용:
- workspace/crate 구성 결과
- 생성된 7개 JsonSchema 구조체 목록과 파일 경로
- Plan 02에서 import할 수 있는 타입 (`executor_core::schema::*`)
- Wave 0 harness 준비 상태 (Plan 02가 바로 테스트 함수를 추가할 수 있음)
- golden schema 파일 초기화 방법 안내 (`UPDATE_SCHEMAS=1 cargo test -p executor-core`)
</output>
</content>
</invoke>