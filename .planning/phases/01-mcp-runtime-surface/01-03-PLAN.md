---
phase: 01-mcp-runtime-surface
plan: 03
type: execute
wave: 3
depends_on: ["01-mcp-runtime-surface/01", "01-mcp-runtime-surface/02"]
files_modified:
  - crates/executor-mcp/src/lib.rs
  - crates/executor-mcp/src/prompts.rs
  - crates/executor-mcp/src/resources.rs
  - crates/executor-mcp/src/server.rs
  - crates/executor-mcp/tests/stdio_handshake.rs
  - crates/executor-core/tests/schemas/WriteEvmStrategyArgs.json
  - crates/executor-core/tests/schemas/ReviewEvmStrategyArgs.json
autonomous: true
requirements:
  - MCP-01
  - MCP-03
  - MCP-04
must_haves:
  truths:
    - "resources/list가 빈 배열을 반환한다 (Phase 2+에서 채워짐)"
    - "resources/templates/list가 세 URI 템플릿(strategy://{strategy_id}, execution://{execution_id}, journal://{execution_id})을 반환한다"
    - "resources/read가 임의 URI에 대해 resource_not_found 에러(-32002)와 data.phase=1을 반환한다"
    - "prompts/list가 두 prompt(write_evm_strategy, review_evm_strategy)를 arguments 스키마와 함께 반환한다"
    - "prompts/get 호출 시 placeholder PromptMessage (Phase 7 finalization 안내 문구)가 반환된다"
    - "통합 테스트가 서버 시작부터 종료까지 stdout으로 들어오는 모든 줄이 valid JSON-RPC 2.0 메시지임을 assert한다"
    - "schema round-trip 테스트가 8 tool input + 2 prompt args 구조체 모두의 JsonSchema가 sample input을 validate함을 확인한다"
  artifacts:
    - path: "crates/executor-mcp/src/prompts.rs"
      provides: "#[prompt_router] impl + write_evm_strategy / review_evm_strategy placeholder bodies"
      contains: "#\\[prompt_router\\]"
    - path: "crates/executor-mcp/src/resources.rs"
      provides: "list_resources / list_resource_templates / read_resource helper functions"
      contains: "strategy://\\{strategy_id\\}"
    - path: "crates/executor-mcp/src/server.rs"
      provides: "ExecutorServer ServerHandler impl — #[tool_handler] + #[prompt_handler] 같은 impl block"
      contains: "#\\[prompt_handler\\]"
    - path: "crates/executor-mcp/tests/stdio_handshake.rs"
      provides: "통합 테스트 4개 추가 (resources_surface, prompts_surface, stdout_is_strict_jsonrpc, schema_contract_round_trip)"
      contains: "stdout_is_strict_jsonrpc"
    - path: "crates/executor-core/tests/schemas/WriteEvmStrategyArgs.json"
      provides: "prompt args golden schema"
      contains: "intent"
    - path: "crates/executor-core/tests/schemas/ReviewEvmStrategyArgs.json"
      provides: "prompt args golden schema"
      contains: "strategy_id"
  key_links:
    - from: "crates/executor-mcp/src/server.rs"
      to: "rmcp::ServerHandler"
      via: "#[tool_handler] + #[prompt_handler]"
      pattern: "impl ServerHandler"
    - from: "crates/executor-mcp/src/prompts.rs"
      to: "executor_core::schema::prompt_args"
      via: "Parameters<WriteEvmStrategyArgs|ReviewEvmStrategyArgs>"
      pattern: "Parameters<.*Args>"
    - from: "crates/executor-mcp/src/resources.rs"
      to: "rmcp::model::ResourceTemplate"
      via: "uri_template 문자열"
      pattern: "uri_template"
---

<objective>
Phase 1 MCP 표면의 마지막 두 축인 resources와 prompts를 추가해 MCP-03, MCP-04를 충족한다. Plan 02가 남긴 `impl ServerHandler` 블록에 `#[prompt_handler]`를 함께 붙이고 (Pitfall 6 준수), `list_resources` / `list_resource_templates` / `read_resource`를 오버라이드한다. 마지막으로 stdout 규율과 schema contract round-trip을 증명하는 2개 테스트를 추가해 phase gate를 통과시킨다.

Purpose: MCP 클라이언트(Claude Desktop, MCP Inspector)에서 tools/resources/prompts 세 축이 모두 비어있지 않고 계약 형태로 노출되는 상태 달성.
Output: 완성된 `executor-mcp` binary, 7개 통합 테스트(Plan 02의 3개 + 본 plan의 4개), 7개 golden schema (Plan 02의 5개 + 본 plan의 2개 prompt args).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mcp-runtime-surface/01-CONTEXT.md
@.planning/phases/01-mcp-runtime-surface/01-RESEARCH.md
@.planning/phases/01-mcp-runtime-surface/01-VALIDATION.md
@.planning/phases/01-mcp-runtime-surface/01-01-SUMMARY.md
@.planning/phases/01-mcp-runtime-surface/01-02-SUMMARY.md

<interfaces>
<!-- Plan 01/02가 생성한 계약 — 그대로 import -->

executor-core::schema::prompt_args::{WriteEvmStrategyArgs, ReviewEvmStrategyArgs}
executor-mcp::server::ExecutorServer (Plan 02: tool_router + prompt_router 초기화 — primary `PromptRouter::new()` 또는 fallback 빈 `#[prompt_router] impl ExecutorServer {}` 스텁 중 Plan 02 done 섹션에 기록된 경로)
executor-mcp::tests::common::{spawn_server, send, recv, initialize}

<!-- Plan 02 Task 2 done 섹션에 기록된 채택 경로에 따라 Plan 03 Task 1의 변경 범위가 달라짐:
     - primary 채택: `ExecutorServer::new()`의 `prompt_router: PromptRouter::new()` 라인을 `Self::prompt_router()`로 교체 + prompts.rs에 `#[prompt_router]` 블록 신규 생성
     - fallback 채택: Plan 02가 이미 빈 `#[prompt_router] impl` 스텁을 두고 `Self::prompt_router()`를 호출하고 있음 → prompts.rs는 그 스텁에 prompt 메서드 2개만 추가(혹은 별도 파일로 분리 후 Plan 02 스텁 제거) -->
</interfaces>

<jsonrpc_error_codes>
<!-- MCP spec 2025-11-25 basic/errors 참조 -->
-32002 = resource_not_found (rmcp::ErrorData::resource_not_found가 생성하는 코드)
-32010 = unimplemented (Plan 02 primary)
-32603 = internal_error (Plan 02 fallback용 wire code — `data.code="unimplemented"`로 구분)
</jsonrpc_error_codes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prompt router + resource handler + server impl 통합</name>
  <read_first>
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Prompt 등록 패턴" — write_evm_strategy / review_evm_strategy 정확한 macro 시그니처 + placeholder body 문자열
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"ServerHandler impl — resource templates + lifecycle" — list_resources / list_resource_templates / read_resource 블록 전체
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Open Questions (RESOLVED)" #5 — `ResourceTemplate` 필드 집합 (uri_template, name, description, mime_type, annotations)
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Known Pitfalls" Pitfall 6 — `#[tool_handler]` + `#[prompt_handler]`가 하나의 impl block에 함께 있어야 함
    - .planning/phases/01-mcp-runtime-surface/01-CONTEXT.md D-03, D-03a, D-04
    - crates/executor-mcp/src/server.rs (Plan 02 상태: #[tool_handler]만, prompt_router primary/fallback 경로 중 하나로 초기화 완료)
    - crates/executor-mcp/src/tools.rs (Plan 02 산출물 — 변경 금지, prompts.rs는 별도 impl block에 #[prompt_router]로 붙임)
    - .planning/phases/01-mcp-runtime-surface/01-02-SUMMARY.md 의 "채택된 PromptRouter 초기화 경로" (Plan 02 Task 2 done)
  </read_first>
  <files>
    - crates/executor-mcp/src/prompts.rs
    - crates/executor-mcp/src/resources.rs
    - crates/executor-mcp/src/server.rs
    - crates/executor-mcp/src/lib.rs
  </files>
  <action>
    **1. `crates/executor-mcp/src/prompts.rs`** — RESEARCH.md §"Prompt 등록 패턴" 그대로:
      ```rust
      use rmcp::{
          ErrorData as McpError, RoleServer,
          handler::server::wrapper::Parameters,
          model::{GetPromptResult, PromptMessage, PromptMessageRole},
          prompt, prompt_router,
          service::RequestContext,
      };
      use executor_core::schema::prompt_args::{ReviewEvmStrategyArgs, WriteEvmStrategyArgs};
      use crate::server::ExecutorServer;

      #[prompt_router]
      impl ExecutorServer {
          #[prompt(
              name = "write_evm_strategy",
              description = "Author a new EVM automation strategy. Body finalized in Phase 7 after ctx API stabilizes."
          )]
          async fn write_evm_strategy(
              &self,
              Parameters(_args): Parameters<WriteEvmStrategyArgs>,
              _ctx: RequestContext<RoleServer>,
          ) -> Result<GetPromptResult, McpError> {
              Ok(GetPromptResult::new(vec![PromptMessage::new_text(
                  PromptMessageRole::User,
                  "Strategy authoring prompt — body will be finalized after ctx API stabilizes (Phase 7).",
              )])
              .with_description("Placeholder authoring prompt"))
          }

          #[prompt(
              name = "review_evm_strategy",
              description = "Review an existing EVM automation strategy. Body finalized in Phase 7."
          )]
          async fn review_evm_strategy(
              &self,
              Parameters(_args): Parameters<ReviewEvmStrategyArgs>,
              _ctx: RequestContext<RoleServer>,
          ) -> Result<GetPromptResult, McpError> {
              Ok(GetPromptResult::new(vec![PromptMessage::new_text(
                  PromptMessageRole::User,
                  "Strategy review prompt — body will be finalized in Phase 7.",
              )])
              .with_description("Placeholder review prompt"))
          }
      }
      ```

      **Plan 02에서 fallback 경로(빈 `#[prompt_router] impl ExecutorServer {}` 스텁)를 채택한 경우**: 해당 스텁을 `crates/executor-mcp/src/prompts.rs`의 위 `#[prompt_router] impl ExecutorServer` 블록으로 통합 또는 교체 (스텁이 `server.rs`에 있었다면 삭제하고 이쪽으로 이동). 두 번째 `#[prompt_router] impl` 블록이 동일 struct에 존재하면 매크로 중복으로 컴파일 실패하므로 반드시 병합.

      **Plan 02에서 primary 경로(`PromptRouter::new()`)를 채택한 경우**: `server.rs`의 `ExecutorServer::new()`에서 `prompt_router: PromptRouter::new()` 라인을 `prompt_router: Self::prompt_router()`로 교체 (위 `#[prompt_router]` 매크로가 `Self::prompt_router()` 연관 함수를 생성).

    > `GetPromptResult::new(...)` 반환 타입은 rmcp 1.5 macro가 요구함. 두 prompt 모두 동일 반환 타입 유지 (`PromptMessage` 단독 vec 반환은 deprecated 시그니처 가능 — GetPromptResult로 통일).

    **2. `crates/executor-mcp/src/resources.rs`** — URI 템플릿 및 read_resource 처리. **RESOLVED #5에 따라 `ResourceTemplate`의 모든 필드를 명시적으로 나열**해 `Default` trait 구현 여부에 의존하지 않는다:

      ```rust
      use rmcp::{
          ErrorData as McpError, RoleServer,
          model::{
              ListResourceTemplatesResult, ListResourcesResult, PaginatedRequestParams,
              ReadResourceRequestParams, ReadResourceResult, ResourceTemplate,
          },
          service::RequestContext,
      };
      use serde_json::json;

      /// rmcp 1.5 `ResourceTemplate` 예상 필드 집합 (RESOLVED #5):
      ///   uri_template: String
      ///   name: String
      ///   description: Option<String>
      ///   mime_type: Option<String>
      ///   annotations: Option<Annotations>
      /// 실제 필드 이름/타입이 다를 경우 executor가 모듈 문서(`cargo doc -p rmcp --open`) 확인 후
      /// Plan 03 done 섹션에 최종 필드 집합을 기록한다.
      fn make_template(
          uri_template: &str,
          name: &str,
          description: &str,
          mime_type: &str,
      ) -> ResourceTemplate {
          ResourceTemplate {
              uri_template: uri_template.into(),
              name: name.into(),
              description: Some(description.into()),
              mime_type: Some(mime_type.into()),
              annotations: None,   // RESOLVED #5: 명시. rmcp 필드가 다르면 executor가 갱신
          }
      }

      pub(crate) async fn list_resources_impl(
          _req: Option<PaginatedRequestParams>,
          _ctx: RequestContext<RoleServer>,
      ) -> Result<ListResourcesResult, McpError> {
          // Phase 1: empty. Phase 2+가 state repo에서 채움.
          Ok(ListResourcesResult {
              resources: Vec::new(),
              next_cursor: None,
              meta: None,
          })
      }

      pub(crate) async fn list_resource_templates_impl(
          _req: Option<PaginatedRequestParams>,
          _ctx: RequestContext<RoleServer>,
      ) -> Result<ListResourceTemplatesResult, McpError> {
          Ok(ListResourceTemplatesResult {
              resource_templates: vec![
                  make_template(
                      "strategy://{strategy_id}",
                      "strategy",
                      "Registered strategy (source + metadata). Populated in Phase 2.",
                      "application/json",
                  ),
                  make_template(
                      "execution://{execution_id}",
                      "execution",
                      "Execution report with status and receipt. Populated in Phase 6.",
                      "application/json",
                  ),
                  make_template(
                      "journal://{execution_id}",
                      "journal",
                      "Journal entries for an execution. Populated in Phase 3+.",
                      "application/json",
                  ),
              ],
              next_cursor: None,
              meta: None,
          })
      }

      pub(crate) async fn read_resource_impl(
          request: ReadResourceRequestParams,
          _ctx: RequestContext<RoleServer>,
      ) -> Result<ReadResourceResult, McpError> {
          // Phase 1: 항상 not-found. URI 스킴 검증/실제 조회는 Phase 2+.
          Err(McpError::resource_not_found(
              "resource not found (Phase 1 placeholder surface — no objects stored yet)",
              Some(json!({ "uri": request.uri, "phase": 1 })),
          ))
      }
      ```

      **Primary 시도:** 위 `make_template` 함수가 컴파일되면 그대로 진행.

      **Fallback 1 — 필드 집합이 다름:** `cargo build -p executor-mcp` 실패 시 rustc 에러 메시지(missing/unknown field)를 보고 필드를 추가/삭제. 예를 들어 rmcp 1.5가 `title: Option<String>` 또는 `icons: Vec<Icon>` 같은 필드를 요구하면 `make_template` 시그니처에 추가하거나 함수 내부에서 `None`/`vec![]`로 초기화. `..Default::default()`는 **사용 금지** (RESOLVED #5: `Default` 가용성 불확실).

      **Fallback 2 — 필드 private:** `ResourceTemplate`이 non-exhaustive이거나 일부 필드가 private이면 rmcp가 제공하는 생성자/빌더(`ResourceTemplate::new(...)`, `ResourceTemplateBuilder` 등)로 전환. executor가 문서 확인 후 채택된 생성 방식과 최종 필드 집합을 Plan 03 done 섹션과 `01-03-SUMMARY.md`에 기록.

    **3. `crates/executor-mcp/src/server.rs` 교체** — Plan 02의 `impl ServerHandler` 블록을 다음으로 교체 (Pitfall 6: `#[tool_handler]` + `#[prompt_handler]`가 같은 impl block에 공존):
      ```rust
      use rmcp::{
          ErrorData as McpError, RoleServer, ServerHandler,
          handler::server::router::{prompt::PromptRouter, tool::ToolRouter},
          model::*,
          prompt_handler,
          service::RequestContext,
          tool_handler,
      };
      use crate::resources;

      #[derive(Clone)]
      pub struct ExecutorServer {
          pub(crate) tool_router: ToolRouter<Self>,
          pub(crate) prompt_router: PromptRouter<Self>,
      }

      impl ExecutorServer {
          pub fn new() -> Self {
              Self {
                  tool_router: Self::tool_router(),
                  prompt_router: Self::prompt_router(), // prompts.rs의 #[prompt_router]가 생성
              }
          }
      }

      impl Default for ExecutorServer {
          fn default() -> Self { Self::new() }
      }

      #[tool_handler]
      #[prompt_handler]
      impl ServerHandler for ExecutorServer {
          fn get_info(&self) -> ServerInfo {
              ServerInfo::new(
                  ServerCapabilities::builder()
                      .enable_tools()
                      .enable_prompts()
                      .enable_resources()
                      .build(),
              )
              .with_server_info(Implementation::from_build_env())
              .with_instructions(
                  "Onchain Strategy MCP — v1 runtime surface. \
                   Write-capable tools return `unimplemented` until their phase lands. \
                   Resource templates declare the strategy/execution/journal URI shapes; no objects stored in Phase 1."
                      .to_string(),
              )
          }

          async fn list_resources(
              &self,
              request: Option<PaginatedRequestParams>,
              ctx: RequestContext<RoleServer>,
          ) -> Result<ListResourcesResult, McpError> {
              resources::list_resources_impl(request, ctx).await
          }

          async fn list_resource_templates(
              &self,
              request: Option<PaginatedRequestParams>,
              ctx: RequestContext<RoleServer>,
          ) -> Result<ListResourceTemplatesResult, McpError> {
              resources::list_resource_templates_impl(request, ctx).await
          }

          async fn read_resource(
              &self,
              request: ReadResourceRequestParams,
              ctx: RequestContext<RoleServer>,
          ) -> Result<ReadResourceResult, McpError> {
              resources::read_resource_impl(request, ctx).await
          }
      }
      ```

    > Plan 02 server.rs의 `#[tool_handler]`만 붙은 버전을 이 블록으로 **전량 교체**. tools.rs / errors.rs / config.rs / logging.rs / main.rs는 변경 없음.

    **4. `crates/executor-mcp/src/lib.rs`** — 모듈 추가:
      ```rust
      #![deny(clippy::print_stdout, clippy::print_stderr, clippy::dbg_macro)]

      pub mod config;
      pub mod errors;
      pub mod logging;
      pub mod prompts;    // NEW
      pub mod resources;  // NEW
      pub mod server;
      pub mod tools;

      pub use server::ExecutorServer;
      ```

    **최종 검증:** `cargo build -p executor-mcp` + `cargo clippy -p executor-mcp --all-targets -- -D warnings` + `cargo test -p executor-mcp --test stdio_handshake` (Plan 02 테스트 3개가 여전히 통과하는지 회귀 검사).
  </action>
  <verify>
    <automated>cargo build -p executor-mcp 2>&amp;1 | grep -q "Finished" &amp;&amp; cargo clippy -p executor-mcp --all-targets -- -D warnings 2>&amp;1 | grep -qE "(Finished|warning:.*0 warnings)" &amp;&amp; grep -q "#\[prompt_router\]" crates/executor-mcp/src/prompts.rs &amp;&amp; grep -q "#\[prompt_handler\]" crates/executor-mcp/src/server.rs &amp;&amp; grep -q "strategy://{strategy_id}" crates/executor-mcp/src/resources.rs &amp;&amp; grep -q "execution://{execution_id}" crates/executor-mcp/src/resources.rs &amp;&amp; grep -q "journal://{execution_id}" crates/executor-mcp/src/resources.rs &amp;&amp; ! grep -q "\.\.Default::default()" crates/executor-mcp/src/resources.rs &amp;&amp; cargo test -p executor-mcp --test stdio_handshake tools_list_emits_full_surface 2>&amp;1 | grep -qE "test result: ok\. 1 passed"</automated>
  </verify>
  <done>
    - `cargo build -p executor-mcp` 성공
    - `cargo clippy -p executor-mcp --all-targets -- -D warnings` 성공
    - `prompts.rs`에 `#[prompt_router]` + 2 prompt handler (`write_evm_strategy`, `review_evm_strategy`)
    - `resources.rs`에 세 URI 템플릿 문자열 (`strategy://{strategy_id}`, `execution://{execution_id}`, `journal://{execution_id}`) 모두 존재
    - `resources.rs`가 **`..Default::default()`를 사용하지 않음** — 모든 `ResourceTemplate` 필드 명시 (RESOLVED #5)
    - **채택된 `ResourceTemplate` 최종 필드 집합 기록** (Wave 3 시작 시점에 아래 체크리스트 채움):
      - [ ] `uri_template: String`
      - [ ] `name: String`
      - [ ] `description: Option<String>`
      - [ ] `mime_type: Option<String>`
      - [ ] `annotations: Option<Annotations>`
      - [ ] 기타 추가 필드(title, icons, size 등): `__________`
      - [ ] 생성 방식: direct struct literal / `ResourceTemplate::new(...)` / builder → 채택: `__________`
    - `server.rs`가 `#[tool_handler]` + `#[prompt_handler]`를 **하나의 impl ServerHandler 블록**에 함께 붙임 (Pitfall 6)
    - `ExecutorServer::new()`가 `Self::prompt_router()`를 호출 (Plan 02 fallback/primary 어느 쪽이든 이 시점에는 정상 동작)
    - Plan 02의 통합 테스트 3개가 회귀 없이 여전히 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: resources/prompts 통합 테스트 + stdout strict JSON-RPC 테스트 + schema round-trip</name>
  <read_first>
    - .planning/phases/01-mcp-runtime-surface/01-VALIDATION.md §"Per-Task Verification Map" — 1-03-01/02/03/04 row (정확한 테스트 함수 이름 + 커맨드)
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Validation Architecture" — stdout assertion 원칙
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Schema Contract Stability Test" — round-trip 패턴
    - crates/executor-mcp/tests/stdio_handshake.rs (Plan 02 상태: tools_list_emits_full_surface / unimplemented_tools_return_phase_hint / readonly_tools_return_placeholder + harness_compiles)
    - crates/executor-core/tests/schema_snapshots.rs (Plan 01 산출물 — WriteEvmStrategyArgs/ReviewEvmStrategyArgs golden이 아직 없음)
  </read_first>
  <files>
    - crates/executor-mcp/tests/stdio_handshake.rs
    - crates/executor-core/tests/schemas/WriteEvmStrategyArgs.json
    - crates/executor-core/tests/schemas/ReviewEvmStrategyArgs.json
  </files>
  <action>
    **1. prompt args golden 생성:**
      ```
      UPDATE_SCHEMAS=1 cargo test -p executor-core --test schema_snapshots write_evm_strategy_args_schema_stable review_evm_strategy_args_schema_stable
      ```
      → `crates/executor-core/tests/schemas/WriteEvmStrategyArgs.json`, `ReviewEvmStrategyArgs.json` 2개 생성. git에 커밋.
      이어서 env 없이 `cargo test -p executor-core --test schema_snapshots` 전체 실행 → 7개 테스트 모두 통과 확인.

    **2. `crates/executor-mcp/tests/stdio_handshake.rs`에 4개 테스트 추가** (VALIDATION.md 1-03-01~04 정확한 이름 준수):

      ```rust
      // VALIDATION.md 1-03-01
      #[tokio::test]
      async fn resources_surface_matches_contract() -> Result<()> {
          let mut proc = spawn_server().await?;
          let _ = initialize(&mut proc).await?;

          // resources/list → 빈 배열
          send(&mut proc, json!({ "jsonrpc":"2.0","id":2,"method":"resources/list" })).await?;
          let r = recv(&mut proc).await?;
          let list = r["result"]["resources"].as_array().expect("resources array");
          assert!(list.is_empty(), "resources/list must be empty in Phase 1, got {list:?}");

          // resources/templates/list → 3개 URI template
          send(&mut proc, json!({ "jsonrpc":"2.0","id":3,"method":"resources/templates/list" })).await?;
          let r = recv(&mut proc).await?;
          let templates = r["result"]["resourceTemplates"].as_array().expect("resourceTemplates array");
          let templates_uris: Vec<&str> = templates.iter()
              .map(|t| t["uriTemplate"].as_str().unwrap())
              .collect();
          assert!(templates_uris.contains(&"strategy://{strategy_id}"));
          assert!(templates_uris.contains(&"execution://{execution_id}"));
          assert!(templates_uris.contains(&"journal://{execution_id}"));
          assert_eq!(templates.len(), 3);

          // resources/read → resource_not_found (-32002) with data.phase=1
          send(&mut proc, json!({
              "jsonrpc":"2.0","id":4,"method":"resources/read",
              "params":{"uri":"strategy://nonexistent"}
          })).await?;
          let r = recv(&mut proc).await?;
          assert_eq!(r["error"]["code"], -32002);
          assert_eq!(r["error"]["data"]["phase"], 1);
          assert_eq!(r["error"]["data"]["uri"], "strategy://nonexistent");

          proc.child.kill().await?;
          Ok(())
      }

      // VALIDATION.md 1-03-02
      #[tokio::test]
      async fn prompts_surface_matches_contract() -> Result<()> {
          let mut proc = spawn_server().await?;
          let _ = initialize(&mut proc).await?;

          // prompts/list → 두 prompt + arguments 스키마
          send(&mut proc, json!({ "jsonrpc":"2.0","id":2,"method":"prompts/list" })).await?;
          let r = recv(&mut proc).await?;
          let prompts = r["result"]["prompts"].as_array().expect("prompts array");
          let names: Vec<&str> = prompts.iter().map(|p| p["name"].as_str().unwrap()).collect();
          assert!(names.contains(&"write_evm_strategy"), "missing write_evm_strategy");
          assert!(names.contains(&"review_evm_strategy"), "missing review_evm_strategy");
          assert_eq!(prompts.len(), 2);
          for p in prompts {
              assert!(p.get("description").is_some(), "prompt {} missing description", p["name"]);
          }

          // prompts/get write_evm_strategy → placeholder PromptMessage
          send(&mut proc, json!({
              "jsonrpc":"2.0","id":3,"method":"prompts/get",
              "params":{"name":"write_evm_strategy","arguments":{"intent":"test intent"}}
          })).await?;
          let r = recv(&mut proc).await?;
          let messages = r["result"]["messages"].as_array().expect("messages array");
          assert!(!messages.is_empty());
          let text = messages[0]["content"]["text"].as_str().unwrap_or("");
          assert!(text.contains("Phase 7") || text.contains("body will be finalized"),
              "placeholder marker missing from write_evm_strategy body: {text}");

          // prompts/get review_evm_strategy → placeholder PromptMessage
          send(&mut proc, json!({
              "jsonrpc":"2.0","id":4,"method":"prompts/get",
              "params":{"name":"review_evm_strategy","arguments":{"strategy_id":"s-1"}}
          })).await?;
          let r = recv(&mut proc).await?;
          let messages = r["result"]["messages"].as_array().expect("messages array");
          assert!(!messages.is_empty());
          let text = messages[0]["content"]["text"].as_str().unwrap_or("");
          assert!(text.contains("Phase 7") || text.contains("body will be finalized"),
              "placeholder marker missing from review_evm_strategy body: {text}");

          proc.child.kill().await?;
          Ok(())
      }

      // VALIDATION.md 1-03-03 — MCP-01 핵심 검증: stdout은 JSON-RPC 전용
      #[tokio::test]
      async fn stdout_is_strict_jsonrpc() -> Result<()> {
          let mut proc = spawn_server().await?;
          let init_resp = initialize(&mut proc).await?;
          assert_eq!(init_resp["jsonrpc"], "2.0");

          for (id, method) in [
              (2, "tools/list"),
              (3, "resources/list"),
              (4, "resources/templates/list"),
              (5, "prompts/list"),
          ] {
              send(&mut proc, json!({ "jsonrpc":"2.0","id":id,"method":method })).await?;
              let r = recv(&mut proc).await?;
              assert_eq!(r["jsonrpc"], "2.0", "method {method}: missing jsonrpc:2.0");
              assert_eq!(r["id"], id, "method {method}: id mismatch");
          }

          send(&mut proc, json!({
              "jsonrpc":"2.0","id":99,"method":"tools/call",
              "params":{"name":"nonexistent_tool","arguments":{}}
          })).await?;
          let r = recv(&mut proc).await?;
          assert!(r.get("error").is_some(), "unknown tool must return JSON-RPC error, not log line");

          proc.child.kill().await?;
          Ok(())
      }

      // VALIDATION.md 1-03-04 — schema round-trip
      #[tokio::test]
      async fn schema_contract_round_trip() -> Result<()> {
          use executor_core::schema::strategy::*;
          use executor_core::schema::execution::ExecutionIdInput;
          use executor_core::schema::policy::PolicyUpdateInput;
          use executor_core::schema::prompt_args::*;

          let cases: Vec<(&str, serde_json::Value)> = vec![
              ("StrategyRegisterInput", json!({"name":"x","source":"// noop"})),
              ("StrategyIdInput", json!({"strategy_id":"s-1"})),
              ("StrategyRunOnceInput", json!({"strategy_id":"s-1"})),
              ("ExecutionIdInput", json!({"execution_id":"e-1"})),
              ("PolicyUpdateInput", json!({})),
              ("WriteEvmStrategyArgs", json!({"intent":"transfer usdc"})),
              ("ReviewEvmStrategyArgs", json!({"strategy_id":"s-1"})),
          ];

          let _: StrategyRegisterInput = serde_json::from_value(cases[0].1.clone())?;
          let _: StrategyIdInput = serde_json::from_value(cases[1].1.clone())?;
          let _: StrategyRunOnceInput = serde_json::from_value(cases[2].1.clone())?;
          let _: ExecutionIdInput = serde_json::from_value(cases[3].1.clone())?;
          let _: PolicyUpdateInput = serde_json::from_value(cases[4].1.clone())?;
          let _: WriteEvmStrategyArgs = serde_json::from_value(cases[5].1.clone())?;
          let _: ReviewEvmStrategyArgs = serde_json::from_value(cases[6].1.clone())?;

          for (name, sample) in &cases {
              assert!(sample.is_object(), "{name}: sample not object");
          }
          Ok(())
      }
      ```

    **3. 전체 회귀 검사:**
      ```
      cargo test -p executor-mcp --test stdio_handshake
      cargo test --workspace
      cargo clippy --workspace --all-targets -- -D warnings
      ```
      모두 통과해야 Phase 1 완료.
  </action>
  <verify>
    <automated>cargo test -p executor-mcp --test stdio_handshake resources_surface_matches_contract 2>&amp;1 | grep -qE "test result: ok\. 1 passed" &amp;&amp; cargo test -p executor-mcp --test stdio_handshake prompts_surface_matches_contract 2>&amp;1 | grep -qE "test result: ok\. 1 passed" &amp;&amp; cargo test -p executor-mcp --test stdio_handshake stdout_is_strict_jsonrpc 2>&amp;1 | grep -qE "test result: ok\. 1 passed" &amp;&amp; cargo test -p executor-mcp --test stdio_handshake schema_contract_round_trip 2>&amp;1 | grep -qE "test result: ok\. 1 passed" &amp;&amp; test -f crates/executor-core/tests/schemas/WriteEvmStrategyArgs.json &amp;&amp; test -f crates/executor-core/tests/schemas/ReviewEvmStrategyArgs.json &amp;&amp; cargo test --workspace 2>&amp;1 | grep -qE "test result: ok" &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings 2>&amp;1 | grep -qE "(Finished|warning:.*0 warnings)"</automated>
  </verify>
  <done>
    - 4개 신규 테스트 통과 (resources_surface_matches_contract / prompts_surface_matches_contract / stdout_is_strict_jsonrpc / schema_contract_round_trip)
    - Plan 02의 3개 테스트도 회귀 없이 통과 (tools_list_emits_full_surface / unimplemented_tools_return_phase_hint / readonly_tools_return_placeholder)
    - 7개 golden schema 파일 존재 (5개 Plan 02 + 2개 Plan 03)
    - `cargo test --workspace` 전체 통과
    - `cargo clippy --workspace --all-targets -- -D warnings` 전체 통과
    - VALIDATION.md `## Validation Sign-Off` 체크리스트 전체 항목 만족
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client ↔ resources/read request uri | URI 문자열이 untrusted — Phase 1은 무조건 not_found로 종료해 파싱/경로 취약점 봉쇄 |
| MCP client ↔ prompts/get arguments | `Parameters<Args>` wrapper가 schemars로 필수 필드 강제 |
| tracing filter level (config.toml) | verbose 로그가 stderr에만 나가도록 Plan 02에서 이미 차단됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | resources/read uri에 path traversal 시도 | mitigate | Phase 1은 URI 파싱 자체를 하지 않고 무조건 resource_not_found 반환. Phase 2 state repo 구현 시 URI 스킴 validation + sanitization 추가 예정 |
| T-01-03-02 | Tampering | stdout 오염 (prompt/resource handler 실수로 println) | mitigate | workspace clippy `print_stdout = "deny"` + `stdout_is_strict_jsonrpc` 테스트가 4가지 method를 rapid-fire하며 모든 응답 라인 JSON-RPC 파싱 검증 |
| T-01-03-03 | Spoofing | 존재하지 않는 tool/prompt 이름으로 호출 시 에러 형태가 깨지거나 panic | mitigate | `stdout_is_strict_jsonrpc`의 nonexistent_tool 테스트가 JSON-RPC error object 반환을 assert |
| T-01-03-04 | Elevation of Privilege | prompt body에 실제 authoring instruction 없이 placeholder만 있음 → agent가 실수로 본 호출 결과를 신뢰 | accept | Phase 7에서 prompt 본문 확정 (D-04). Phase 1 placeholder 문구에 "Phase 7" 명시로 agent 오인 방지 |
| T-01-03-05 | Tampering | schema drift (prompt args 구조체 변경이 agent 쪽 계약 깨뜨림) | mitigate | `tests/schemas/*.json` golden + `schema_snapshots` 테스트가 변경을 의식적 행위로 강제 (PITFALLS Pitfall 3) |

</threat_model>

<verification>
- `cargo test --workspace` 전체 통과 (Plan 01/02/03 누적 테스트)
- `cargo test -p executor-mcp --test stdio_handshake` 7개 테스트 모두 통과 (harness_compiles + Plan 02의 3개 + Plan 03의 4개)
- `cargo test -p executor-core --test schema_snapshots` 7개 테스트 모두 통과
- `cargo clippy --workspace --all-targets -- -D warnings` 통과 — stdout 규율 정적 검증
- `ls crates/executor-core/tests/schemas/*.json | wc -l` = 7
- VALIDATION.md `## Per-Task Verification Map` 모든 row 상태 ✅ green
- VALIDATION.md `## Validation Sign-Off` 전 항목 체크 가능
</verification>

<success_criteria>
- `resources/list` = 빈 배열, `resources/templates/list` = 3개 URI 템플릿(strategy/execution/journal), `resources/read` = -32002 + data.phase=1 (MCP-03)
- `prompts/list` = 2개 prompt (write_evm_strategy, review_evm_strategy) + arguments 스키마, `prompts/get` = Phase 7 안내 문구가 포함된 placeholder PromptMessage (MCP-04)
- stdout strict JSON-RPC 테스트가 4 method rapid-fire + unknown tool 에러 라인 모두 JSON-RPC 2.0으로 파싱됨을 assert (MCP-01)
- schema contract round-trip 테스트가 7개 input struct의 serde↔JsonSchema round-trip을 증명 (MCP-02)
- Phase 1 모든 성공 기준(MCP 클라이언트 initialize, 8 tool + 2 prompt + 3 resource template 노출) 충족
- Claude Desktop / MCP Inspector 수동 smoke test는 VALIDATION.md에 manual 항목으로 남아있음 (Phase gate check-in은 자동 테스트만으로 완료)
</success_criteria>

<output>
완료 후 `.planning/phases/01-mcp-runtime-surface/01-03-SUMMARY.md` 생성. 포함 내용:
- 최종 MCP 표면 요약 (8 tools + 2 prompts + 3 resource templates)
- **채택된 `ResourceTemplate` 최종 필드 집합 + 생성 방식(struct literal/new/builder)** — Phase 2+ 가 resource 구현 시 그대로 재사용
- Phase 2 이후가 참조해야 할 안정 계약: executor-core::schema::* 구조체 목록, `unimplemented_err` helper, `Signer` trait 경계
- Phase 1 통합 테스트 7개 함수 이름 (Phase 2+가 회귀 방지용으로 유지해야 함)
- Phase 2 시작 전 필요한 작업 힌트: `[state]` config 섹션 추가, `executor-state`의 실제 repo trait, `strategy_register` handler가 `executor-state`를 소비하는 형태로 변경
- VALIDATION.md `nyquist_compliant: true` 설정 준비 완료 (모든 row green)
</output>
</content>
