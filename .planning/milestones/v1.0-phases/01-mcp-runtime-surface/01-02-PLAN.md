---
phase: 01-mcp-runtime-surface
plan: 02
type: execute
wave: 2
depends_on: ["01-mcp-runtime-surface/01"]
files_modified:
  - crates/executor-mcp/src/lib.rs
  - crates/executor-mcp/src/server.rs
  - crates/executor-mcp/src/tools.rs
  - crates/executor-mcp/src/errors.rs
  - crates/executor-mcp/src/config.rs
  - crates/executor-mcp/src/logging.rs
  - crates/executor-mcp/src/main.rs
  - crates/executor-mcp/tests/stdio_handshake.rs
  - crates/executor-core/tests/schemas/StrategyRegisterInput.json
  - crates/executor-core/tests/schemas/StrategyIdInput.json
  - crates/executor-core/tests/schemas/StrategyRunOnceInput.json
  - crates/executor-core/tests/schemas/ExecutionIdInput.json
  - crates/executor-core/tests/schemas/PolicyUpdateInput.json
  - config.example.toml
autonomous: true
requirements:
  - MCP-01
  - MCP-02
must_haves:
  truths:
    - "MCP 클라이언트가 stdio로 initialize를 성공시킨다"
    - "tools/list가 8개 tool(strategy_register, strategy_list, strategy_get, strategy_delete, strategy_run_once, execution_get, policy_get, policy_update)을 JSON Schema와 함께 반환한다"
    - "쓰기성 tool(strategy_register, strategy_delete, strategy_run_once, policy_update) 호출 시 data.code=\"unimplemented\" + data.phase=<N>을 포함하는 구조화 에러가 반환된다"
    - "읽기성 tool 중 strategy_list는 빈 배열, policy_get은 placeholder 객체를 성공 응답으로 반환한다"
    - "서버는 stderr로만 tracing 로그를 쓴다 (stdout은 JSON-RPC 2.0 전용)"
    - "config.toml이 없어도 기본값(logging.level=info)으로 부팅한다"
  artifacts:
    - path: "crates/executor-mcp/src/server.rs"
      provides: "ExecutorServer struct + ServerHandler::get_info + #[tool_handler] 선언"
      contains: "struct ExecutorServer"
    - path: "crates/executor-mcp/src/tools.rs"
      provides: "#[tool_router] impl + 8개 tool handler"
      contains: "#\\[tool_router\\]"
    - path: "crates/executor-mcp/src/errors.rs"
      provides: "unimplemented_err(tool_name, phase) helper — ErrorCode(-32010) 또는 McpError::internal_error fallback + structured data"
      contains: "unimplemented"
    - path: "crates/executor-mcp/src/config.rs"
      provides: "Config/LoggingConfig structs + load() (우선순위: --config > EXECUTOR_CONFIG > ./config.toml > 기본값)"
      contains: "EXECUTOR_CONFIG"
    - path: "crates/executor-mcp/src/logging.rs"
      provides: "tracing subscriber stderr-only 초기화"
      contains: "with_writer\\(std::io::stderr\\)"
    - path: "crates/executor-mcp/src/main.rs"
      provides: "tokio::main entry — config::load → logging::init → ExecutorServer::new().serve(stdio())"
      contains: "serve\\(stdio\\(\\)\\)"
    - path: "config.example.toml"
      provides: "주석 달린 샘플 config (logging.level만)"
      contains: "\\[logging\\]"
  key_links:
    - from: "crates/executor-mcp/src/main.rs"
      to: "rmcp::transport::stdio"
      via: "ServiceExt::serve"
      pattern: "serve\\(stdio\\(\\)\\)"
    - from: "crates/executor-mcp/src/tools.rs"
      to: "executor_core::schema::*"
      via: "Parameters<T: JsonSchema> wrapper"
      pattern: "Parameters<.*Input>"
    - from: "crates/executor-mcp/src/logging.rs"
      to: "std::io::stderr"
      via: "tracing_subscriber fmt layer with_writer"
      pattern: "with_writer\\(std::io::stderr\\)"
    - from: "crates/executor-mcp/tests/stdio_handshake.rs"
      to: "crates/executor-mcp/src/tools.rs"
      via: "spawn bin + tools/list round-trip"
      pattern: "tools/list"
---

<objective>
rmcp 1.5 macro를 사용해 `ExecutorServer`를 구현한다. 8개 tool(4개 쓰기성 + 4개 읽기성)을 `#[tool_router]`로 선언하고, stdio transport로 서빙한다. Config 로더와 stderr-only tracing을 연결해 MCP-01(stdout 규율) + MCP-02(tool surface)를 동시에 충족한다. Plan 01이 남긴 integration test harness에 `tools/list` / `tools/call` 테스트를 추가해 계약을 증명한다.

Purpose: Phase 1의 MCP tool 표면을 **계약 수준으로 확정**한다. 스키마가 agent에게 전파되기 때문에 이 단계에서 고쳐둬야 나중 변경 비용이 작다.
Output: 빌드 가능한 `executor-mcp` binary, 3개 통합 테스트(tools_list_emits_full_surface / unimplemented_tools_return_phase_hint / readonly_tools_return_placeholder), 7개 schema golden file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mcp-runtime-surface/01-CONTEXT.md
@.planning/phases/01-mcp-runtime-surface/01-RESEARCH.md
@.planning/phases/01-mcp-runtime-surface/01-VALIDATION.md
@.planning/phases/01-mcp-runtime-surface/01-01-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- Plan 01이 생성한 계약. 이 plan은 이 타입들을 그대로 import 한다. -->

executor-core::schema::strategy::{StrategyRegisterInput, StrategyIdInput, StrategyRunOnceInput}
executor-core::schema::execution::ExecutionIdInput
executor-core::schema::policy::PolicyUpdateInput
executor-core::schema::prompt_args::{WriteEvmStrategyArgs, ReviewEvmStrategyArgs} (Plan 03 사용)

executor-mcp::tests::common::{ServerProc, spawn_server, send, recv, initialize} (Plan 01 Wave 0 harness)

<!-- 이 plan이 **새로 생성**하여 Plan 03가 소비할 계약 -->

executor_mcp::errors::unimplemented_err(tool_name: &'static str, phase: u8) -> McpError
executor_mcp::config::{Config, LoggingConfig, load() -> Result<Config>}
executor_mcp::logging::init(&Config) -> Result<()>
executor_mcp::server::ExecutorServer (#[tool_handler] 포함, prompt_handler는 Plan 03가 추가)
</interfaces>

<unimplemented_phase_map>
<!-- D-02a: Unimplemented error의 phase 숫자는 roadmap 의존성 따라 명시 -->
strategy_register  → phase: 2  (STR-01, Phase 2: Strategy State and Journal)
strategy_delete    → phase: 2  (STR-02, Phase 2)
strategy_run_once  → phase: 6  (전체 runtime loop 완성 시점)
policy_update      → phase: 5  (POL-01..06, Phase 5: Simulation and Policy Gate)
</unimplemented_phase_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config 로더 + stderr-only tracing + main() 와이어링</name>
  <read_first>
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Config Loading Pattern" (Config struct + load() fn 전체 코드)
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Logging init" (with_writer(std::io::stderr) 포함)
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"main() entry point"
    - .planning/phases/01-mcp-runtime-surface/01-CONTEXT.md D-05, D-05a, D-06, D-06a, D-06b
    - crates/executor-mcp/src/main.rs (Plan 01 stub)
  </read_first>
  <files>
    - crates/executor-mcp/src/config.rs
    - crates/executor-mcp/src/logging.rs
    - crates/executor-mcp/src/main.rs
    - crates/executor-mcp/src/lib.rs
    - config.example.toml
  </files>
  <action>
    **`crates/executor-mcp/src/config.rs`** — RESEARCH.md §"Config Loading Pattern" 블록 그대로 구현:
      - `#[derive(Debug, Clone, Deserialize)] #[serde(deny_unknown_fields)] pub struct Config { #[serde(default)] pub logging: LoggingConfig }`.
      - `#[derive(Debug, Clone, Deserialize)] #[serde(deny_unknown_fields)] pub struct LoggingConfig { #[serde(default = "default_log_level")] pub level: String }`.
      - `fn default_log_level() -> String { "info".into() }`.
      - `impl Default for Config`, `impl Default for LoggingConfig`.
      - `pub fn load() -> anyhow::Result<Config>` — 우선순위: `--config <path>` CLI arg > `EXECUTOR_CONFIG` env > `./config.toml` > 내장 기본값 (D-06b). 파일이 존재하지 않으면 `Config::default()` 반환. 존재하면 `toml::from_str` + `anyhow::Context`로 에러 래핑.

    **`crates/executor-mcp/src/logging.rs`** — RESEARCH.md §"Logging init" 그대로:
      - `pub fn init(cfg: &crate::config::Config) -> anyhow::Result<()>`.
      - `EnvFilter::try_from_default_env().unwrap_or_else(|_| EnvFilter::new(&cfg.logging.level))`.
      - `tracing_subscriber::registry().with(env_filter).with(fmt::layer().with_writer(std::io::stderr)).init()`.
      - **CRITICAL:** `with_writer(std::io::stderr)` 누락 금지 (D-05: stdout은 JSON-RPC 전용).

    **`crates/executor-mcp/src/lib.rs`** (Plan 01에서 placeholder 상태) — 전체 교체:
      ```rust
      #![deny(clippy::print_stdout, clippy::print_stderr, clippy::dbg_macro)]

      pub mod config;
      pub mod errors;    // Task 2가 생성
      pub mod logging;
      pub mod server;    // Task 2가 생성
      pub mod tools;     // Task 2가 생성

      pub use server::ExecutorServer;
      ```
      (Task 2 실행 전에도 이 선언은 그대로 두고, `errors`/`server`/`tools` 모듈은 Task 2에서 채운다. Task 1만으로는 `cargo check` 실패 가능 — 이 Task의 verify는 `cargo check -p executor-mcp` 대신 config/logging 단위 테스트만 본다.)

      실제로는 Task 1과 Task 2를 하나의 커밋 단위로 묶어도 좋지만 plan 레벨에서는 Task 분리 유지 — 최종 `cargo check`는 Task 2의 verify에서.

    **`crates/executor-mcp/src/main.rs`** — RESEARCH.md §"main() entry point" 그대로:
      ```rust
      #![deny(clippy::print_stdout, clippy::print_stderr, clippy::dbg_macro)]

      use anyhow::Result;
      use executor_mcp::{ExecutorServer, config, logging};
      use rmcp::{ServiceExt, transport::stdio};

      #[tokio::main]
      async fn main() -> Result<()> {
          let cfg = config::load()?;
          logging::init(&cfg)?;
          tracing::info!(version = env!("CARGO_PKG_VERSION"), "executor-mcp starting");
          let service = ExecutorServer::new().serve(stdio()).await?;
          service.waiting().await?;
          Ok(())
      }
      ```

    **`config.example.toml`** (project root):
      ```toml
      # Phase 1 config surface. Phase 2+에서 [state], [evm], [policy], [signer] 섹션이 추가됨.
      [logging]
      level = "info"   # trace | debug | info | warn | error
      ```
  </action>
  <verify>
    <automated>grep -q "with_writer(std::io::stderr)" crates/executor-mcp/src/logging.rs &amp;&amp; grep -q "EXECUTOR_CONFIG" crates/executor-mcp/src/config.rs &amp;&amp; grep -q "deny_unknown_fields" crates/executor-mcp/src/config.rs &amp;&amp; grep -q "serve(stdio())" crates/executor-mcp/src/main.rs &amp;&amp; grep -q "#!\[deny(clippy::print_stdout" crates/executor-mcp/src/main.rs &amp;&amp; test -f config.example.toml</automated>
  </verify>
  <done>
    - config.rs에 Config/LoggingConfig + load() 함수 존재
    - logging.rs가 `with_writer(std::io::stderr)`를 명시적으로 사용 (D-05 핵심)
    - main.rs가 `config::load → logging::init → ExecutorServer::new().serve(stdio())` 순서로 와이어링
    - `#[serde(deny_unknown_fields)]`로 Phase 2+ 필드 추가 시 감지 (D-06)
    - config.example.toml이 project root에 존재 + logging.level 주석 포함
  </done>
</task>

<task type="auto">
  <name>Task 2: ExecutorServer + 8 tool handler + unimplemented_err helper</name>
  <read_first>
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"rmcp 1.5 API Reference" — Import surface, Tool 등록 패턴 전체 블록, ServerHandler impl 블록
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Unimplemented Error Shape" — ErrorCode(-32010) + data payload shape 전체
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Open Questions (RESOLVED)" #4 (ErrorCode 생성자 fallback) + #5 (PromptRouter::new() fallback)
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Known Pitfalls" — Pitfall 4 (fn visibility), Pitfall 5 (enable_* capabilities), Pitfall 6 (tool_handler + prompt_handler 같은 impl block), Pitfall 7 (tool name collision)
    - .planning/phases/01-mcp-runtime-surface/01-CONTEXT.md D-02, D-02a, D-02b, D-02c
    - crates/executor-mcp/src/lib.rs (Task 1 완료 상태)
  </read_first>
  <files>
    - crates/executor-mcp/src/errors.rs
    - crates/executor-mcp/src/server.rs
    - crates/executor-mcp/src/tools.rs
  </files>
  <action>
    **`crates/executor-mcp/src/errors.rs`** — RESOLVED #4에 따라 **primary 경로와 fallback 경로를 모두 시도**:

    **Primary 시도** (RESEARCH.md §"Unimplemented Error Shape"):
      ```rust
      use rmcp::{ErrorData as McpError, model::ErrorCode};
      use serde_json::json;

      /// -32010: unimplemented feature. JSON-RPC server-defined range (-32000..-32099).
      const UNIMPLEMENTED_CODE: ErrorCode = ErrorCode(-32010);

      pub fn unimplemented_err(tool_name: &'static str, phase: u8) -> McpError {
          McpError::new(
              UNIMPLEMENTED_CODE,
              format!("{tool_name} is not implemented yet (lands in Phase {phase})"),
              Some(json!({
                  "code": "unimplemented",
                  "tool": tool_name,
                  "phase": phase,
                  "hint": format!("will be implemented when Phase {phase} lands"),
              })),
          )
      }
      ```

    **Fallback 경로** (RESOLVED #4: rmcp의 `ErrorCode`가 tuple 생성자를 공개하지 않으면): `ErrorCode::from(-32010_i32)` 또는 `ErrorCode::try_from(-32010)` 시도 → 이마저 실패하면 **서버 측 구조화 internal_error로 대체**:
      ```rust
      pub fn unimplemented_err(tool_name: &'static str, phase: u8) -> McpError {
          McpError::internal_error(
              "unimplemented",
              Some(json!({
                  "code": "unimplemented",
                  "tool": tool_name,
                  "phase": phase,
                  "hint": format!("will be implemented when Phase {phase} lands"),
              })),
          )
      }
      ```
      이 fallback은 wire-level JSON-RPC code가 `-32603`(Internal error)으로 바뀌지만, `data.code="unimplemented"` + `data.tool` + `data.phase`는 동일하게 유지되므로 integration test의 assert 4필드 중 3필드(`data.code`, `data.tool`, `data.phase`)는 변하지 않음. **유일하게 바뀌는 것은 `error.code` 숫자값**.

    **실행 순서:** executor는 primary 경로를 시도해 `cargo check -p executor-mcp`를 돌리고, 컴파일 에러 발생 시 fallback 경로로 교체. 최종 채택된 wire-level code(`-32010` 또는 `-32603`)를 이 PLAN.md의 `done` 섹션에 기록한다(Wave 2 시작 시점). 동시에 Task 3의 `unimplemented_tools_return_phase_hint` 테스트의 `assert_eq!(err["code"], -32010, ...)` 라인도 실제 채택된 값으로 갱신한다.

    **`crates/executor-mcp/src/server.rs`** — RESEARCH.md §"ServerHandler impl" 블록 기반:
      ```rust
      use rmcp::{
          ErrorData as McpError, RoleServer, ServerHandler,
          handler::server::router::{prompt::PromptRouter, tool::ToolRouter},
          model::*,
          service::RequestContext,
          tool_handler,
      };

      #[derive(Clone)]
      pub struct ExecutorServer {
          pub(crate) tool_router: ToolRouter<Self>,
          pub(crate) prompt_router: PromptRouter<Self>,
      }

      impl ExecutorServer {
          pub fn new() -> Self {
              Self {
                  tool_router: Self::tool_router(),   // generated by #[tool_router] in tools.rs
                  prompt_router: Self::prompt_router(), // see fallback note below
              }
          }
      }

      impl Default for ExecutorServer {
          fn default() -> Self { Self::new() }
      }

      // NOTE: Plan 03가 이 블록에 #[prompt_handler]를 추가한다.
      // Pitfall 6 — tool_handler와 prompt_handler는 하나의 impl ServerHandler 블록에 함께 있어야 한다.
      #[tool_handler]
      impl ServerHandler for ExecutorServer {
          fn get_info(&self) -> ServerInfo {
              ServerInfo::new(
                  ServerCapabilities::builder()
                      .enable_tools()
                      .enable_prompts()
                      .enable_resources()
                      .build(),
              )
              .with_server_info(Implementation::from_build_env())
              .with_instructions(
                  "Onchain Strategy MCP — v1 runtime surface. \
                   Write-capable tools return `unimplemented` (code -32010 or -32603 fallback) until their phase lands."
                      .to_string(),
              )
          }
      }
      ```

    **PromptRouter 초기화 — RESOLVED #5에 따른 primary + fallback (중요):**

    - **Primary:** `prompt_router: PromptRouter::new()` 를 먼저 시도. 공개 생성자이면 Plan 03가 `Self::prompt_router()`로 한 줄만 교체.
    - **Fallback:** `PromptRouter::new()`가 private/존재하지 않으면 **Plan 02에서 빈 `#[prompt_router] impl ExecutorServer { }` 스텁 블록을 추가** (메서드 없음). 이렇게 하면 `#[prompt_router]` 매크로가 `Self::prompt_router()` 연관 함수를 즉시 생성하므로, `ExecutorServer::new()`에서 곧바로 `prompt_router: Self::prompt_router()`를 호출 가능. Plan 03는 이 스텁 impl 블록에 실제 prompt 메서드(`write_evm_strategy`, `review_evm_strategy`)만 추가하면 됨.

    두 경로 모두 Plan 03의 변경 범위는 최소화된다(primary=한 줄 교체 + prompt 메서드 2개 추가 / fallback=prompt 메서드 2개 추가만).

    executor는 primary를 먼저 시도하고, 컴파일 실패 시 fallback(빈 `#[prompt_router]` impl) 경로로 전환. 채택된 경로를 이 PLAN.md `done` 섹션에 기록.

    **`crates/executor-mcp/src/tools.rs`** — RESEARCH.md §"Tool 등록 패턴" 그대로 (이하 Plan 01 원문 유지, 8 tool handler 선언).

    **최종 검증:** `cargo build -p executor-mcp` 성공 + `cargo clippy -p executor-mcp --all-targets -- -D warnings` 성공.
  </action>
  <verify>
    <automated>cargo build -p executor-mcp 2>&amp;1 | grep -q "Finished" &amp;&amp; cargo clippy -p executor-mcp --all-targets -- -D warnings 2>&amp;1 | grep -qE "(Finished|warning:.*0 warnings)" &amp;&amp; grep -q "strategy_register" crates/executor-mcp/src/tools.rs &amp;&amp; grep -q "strategy_list" crates/executor-mcp/src/tools.rs &amp;&amp; grep -q "policy_get" crates/executor-mcp/src/tools.rs &amp;&amp; grep -q "policy_update" crates/executor-mcp/src/tools.rs &amp;&amp; grep -q "execution_get" crates/executor-mcp/src/tools.rs &amp;&amp; grep -E "ErrorCode\(-32010\)|McpError::internal_error.*\"unimplemented\"" crates/executor-mcp/src/errors.rs &amp;&amp; grep -E "PromptRouter::new\(\)|#\[prompt_router\]" crates/executor-mcp/src/server.rs crates/executor-mcp/src/tools.rs crates/executor-mcp/src/prompts.rs 2>/dev/null | grep -qE "PromptRouter::new\(\)|#\[prompt_router\]" &amp;&amp; grep -q "enable_prompts" crates/executor-mcp/src/server.rs &amp;&amp; grep -q "enable_resources" crates/executor-mcp/src/server.rs</automated>
  </verify>
  <done>
    - `cargo build -p executor-mcp` 성공
    - `cargo clippy -p executor-mcp --all-targets -- -D warnings` 성공
    - 8개 tool 이름(`strategy_register`, `strategy_list`, `strategy_get`, `strategy_delete`, `strategy_run_once`, `execution_get`, `policy_get`, `policy_update`) 모두 tools.rs에 존재
    - `#[tool(name = "...")]` attribute가 모든 handler에 명시되어 있음 (Pitfall 7 방지)
    - **채택된 unimplemented error 생성 경로 기록** (Wave 2 시작 시점에 아래 한 줄을 확정 기입):
      - [ ] primary: `ErrorCode(-32010)` tuple 생성자 사용 → wire code = **-32010**
      - [ ] fallback: `McpError::internal_error("unimplemented", ...)` 사용 → wire code = **-32603**
      - 최종 채택: `__________` (executor 기입)
    - **채택된 PromptRouter 초기화 경로 기록** (Wave 2 시작 시점):
      - [ ] primary: `PromptRouter::new()` 공개 생성자
      - [ ] fallback: 빈 `#[prompt_router] impl ExecutorServer {}` 스텁 + `Self::prompt_router()` 호출
      - 최종 채택: `__________` (executor 기입)
    - server.rs에 `enable_tools`, `enable_prompts`, `enable_resources` 세 capability 모두 선언 (Pitfall 5 방지)
    - 쓰기성 tool 4개는 `Err(unimplemented_err(...))` 반환, 각 phase 숫자(2/2/6/5)가 context unimplemented_phase_map과 일치
    - Task 3 테스트의 `assert_eq!(err["code"], ...)` 라인이 실제 채택된 wire code(-32010 또는 -32603)와 일치하도록 갱신됨
  </done>
</task>

<task type="auto">
  <name>Task 3: stdio_handshake 통합 테스트 3개 + schema golden 생성</name>
  <read_first>
    - .planning/phases/01-mcp-runtime-surface/01-RESEARCH.md §"Validation Architecture" — `stdout_is_pure_jsonrpc`, `tools_list_returns_full_surface`, `write_tools_return_unimplemented` 예제 전체
    - .planning/phases/01-mcp-runtime-surface/01-VALIDATION.md §"Per-Task Verification Map" 1-02-01/02/03 row — 정확한 테스트 함수 이름
    - crates/executor-mcp/tests/common/mod.rs (Plan 01 산출물: spawn_server/send/recv/initialize)
    - crates/executor-mcp/tests/stdio_handshake.rs (Plan 01 산출물: mod common + harness_compiles)
    - crates/executor-core/tests/schema_snapshots.rs (Plan 01 산출물 — golden 없으면 panic하는 상태)
    - Task 2의 `done` 섹션에 기록된 **채택된 unimplemented wire code** (−32010 또는 −32603)
  </read_first>
  <files>
    - crates/executor-mcp/tests/stdio_handshake.rs
    - crates/executor-core/tests/schemas/StrategyRegisterInput.json
    - crates/executor-core/tests/schemas/StrategyIdInput.json
    - crates/executor-core/tests/schemas/StrategyRunOnceInput.json
    - crates/executor-core/tests/schemas/ExecutionIdInput.json
    - crates/executor-core/tests/schemas/PolicyUpdateInput.json
  </files>
  <action>
    **1. schema golden 초기화:**
      ```
      UPDATE_SCHEMAS=1 cargo test -p executor-core --test schema_snapshots
      ```
      이 명령이 `crates/executor-core/tests/schemas/*.json` 5개 tool input golden을 생성한다 (prompt args 2개는 Plan 03 완료 후 Plan 03 Task가 생성). 생성된 JSON은 git에 커밋.
      이어서 `cargo test -p executor-core --test schema_snapshots`를 env 없이 재실행해 golden이 안정적임을 확인 (첫 실행 UPDATE로 쓰고 두 번째는 읽기만).

    **2. `crates/executor-mcp/tests/stdio_handshake.rs`** — 기존 `harness_compiles` 테스트 유지 + 3개 테스트 추가. **Task 2의 done 섹션에 기록된 채택 wire code**를 `EXPECTED_UNIMPL_CODE` 상수로 상단에 선언해 둘 다 커버:

      ```rust
      use anyhow::Result;
      use serde_json::{json, Value};
      mod common;
      use common::{initialize, recv, send, spawn_server};

      // Task 2 done 섹션에서 확정된 wire code. -32010(primary) 또는 -32603(internal_error fallback).
      // Wave 2 시작 시점에 executor가 이 값을 갱신한다.
      const EXPECTED_UNIMPL_CODE: i64 = -32010;   // ← 필요 시 -32603으로 교체

      // VALIDATION.md 1-02-01
      #[tokio::test]
      async fn tools_list_emits_full_surface() -> Result<()> {
          let mut proc = spawn_server().await?;
          let _ = initialize(&mut proc).await?;

          send(&mut proc, json!({ "jsonrpc": "2.0", "id": 2, "method": "tools/list" })).await?;
          let r = recv(&mut proc).await?;
          let tools = r["result"]["tools"].as_array().expect("tools array");
          let names: Vec<&str> = tools.iter().map(|t| t["name"].as_str().unwrap()).collect();
          for expected in [
              "strategy_register", "strategy_list", "strategy_get", "strategy_delete",
              "strategy_run_once", "execution_get", "policy_get", "policy_update",
          ] {
              assert!(names.contains(&expected), "missing tool: {expected} — got: {names:?}");
          }
          assert_eq!(tools.len(), 8, "expected exactly 8 tools, got {}", tools.len());
          for t in tools {
              assert!(t.get("inputSchema").is_some(), "tool {} missing inputSchema", t["name"]);
              assert!(t.get("description").is_some(), "tool {} missing description", t["name"]);
          }
          proc.child.kill().await?;
          Ok(())
      }

      // VALIDATION.md 1-02-02
      #[tokio::test]
      async fn unimplemented_tools_return_phase_hint() -> Result<()> {
          let cases = [
              ("strategy_register", 2_u64),
              ("strategy_delete",   2_u64),
              ("strategy_run_once", 6_u64),
              ("policy_update",     5_u64),
          ];
          for (tool, expected_phase) in cases {
              let mut proc = spawn_server().await?;
              let _ = initialize(&mut proc).await?;

              let args = match tool {
                  "strategy_register" => json!({ "name": "x", "source": "// noop" }),
                  "strategy_delete"   => json!({ "strategy_id": "s-1" }),
                  "strategy_run_once" => json!({ "strategy_id": "s-1" }),
                  "policy_update"     => json!({}),
                  _ => unreachable!(),
              };
              send(&mut proc, json!({
                  "jsonrpc": "2.0", "id": 2, "method": "tools/call",
                  "params": { "name": tool, "arguments": args }
              })).await?;
              let r = recv(&mut proc).await?;
              let err = &r["error"];
              assert_eq!(err["code"], EXPECTED_UNIMPL_CODE,
                  "tool {tool}: expected code {EXPECTED_UNIMPL_CODE}, got {}", err["code"]);
              assert_eq!(err["data"]["code"], "unimplemented", "tool {tool}: data.code");
              assert_eq!(err["data"]["phase"], expected_phase,
                  "tool {tool}: expected phase {expected_phase}, got {}", err["data"]["phase"]);
              assert_eq!(err["data"]["tool"], tool, "tool {tool}: data.tool mismatch");
              proc.child.kill().await?;
          }
          Ok(())
      }

      // VALIDATION.md 1-02-03
      #[tokio::test]
      async fn readonly_tools_return_placeholder() -> Result<()> {
          let mut proc = spawn_server().await?;
          let _ = initialize(&mut proc).await?;

          send(&mut proc, json!({
              "jsonrpc": "2.0", "id": 2, "method": "tools/call",
              "params": { "name": "strategy_list", "arguments": {} }
          })).await?;
          let r = recv(&mut proc).await?;
          let content = r["result"]["content"][0]["text"].as_str().unwrap();
          let list: Value = serde_json::from_str(content)?;
          assert!(list.is_array() &amp;&amp; list.as_array().unwrap().is_empty(), "strategy_list must return []");

          send(&mut proc, json!({
              "jsonrpc": "2.0", "id": 3, "method": "tools/call",
              "params": { "name": "policy_get", "arguments": {} }
          })).await?;
          let r = recv(&mut proc).await?;
          let content = r["result"]["content"][0]["text"].as_str().unwrap();
          let policy: Value = serde_json::from_str(content)?;
          assert!(policy["chains"].is_array());
          assert!(policy["targets"].is_array());
          assert!(policy["selectors"].is_array());

          send(&mut proc, json!({
              "jsonrpc": "2.0", "id": 4, "method": "tools/call",
              "params": { "name": "strategy_get", "arguments": { "strategy_id": "none" } }
          })).await?;
          let r = recv(&mut proc).await?;
          assert_eq!(r["error"]["data"]["phase"], 2);

          send(&mut proc, json!({
              "jsonrpc": "2.0", "id": 5, "method": "tools/call",
              "params": { "name": "execution_get", "arguments": { "execution_id": "none" } }
          })).await?;
          let r = recv(&mut proc).await?;
          assert_eq!(r["error"]["data"]["phase"], 6);

          proc.child.kill().await?;
          Ok(())
      }
      ```

    세 테스트 모두 `common::recv`가 `serde_json::from_str` 실패 시 panic → stdout 오염 즉시 감지 (MCP-01 간접 검증).

    Plan 01의 `harness_compiles` 테스트는 사실상 `tools_list_emits_full_surface`로 대체되지만, 삭제하면 Plan 01 계약이 깨지므로 유지 (빠른 sanity check).
  </action>
  <verify>
    <automated>cargo test -p executor-mcp --test stdio_handshake tools_list_emits_full_surface -- --nocapture 2>&amp;1 | grep -qE "test result: ok\. 1 passed" &amp;&amp; cargo test -p executor-mcp --test stdio_handshake unimplemented_tools_return_phase_hint 2>&amp;1 | grep -qE "test result: ok\. 1 passed" &amp;&amp; cargo test -p executor-mcp --test stdio_handshake readonly_tools_return_placeholder 2>&amp;1 | grep -qE "test result: ok\. 1 passed" &amp;&amp; test -f crates/executor-core/tests/schemas/StrategyRegisterInput.json &amp;&amp; cargo test -p executor-core --test schema_snapshots 2>&amp;1 | grep -qE "test result: ok\."</automated>
  </verify>
  <done>
    - 3개 통합 테스트 모두 통과 (tools_list_emits_full_surface / unimplemented_tools_return_phase_hint / readonly_tools_return_placeholder)
    - 5개 golden schema 파일이 git-tracked (`StrategyRegisterInput.json`, `StrategyIdInput.json`, `StrategyRunOnceInput.json`, `ExecutionIdInput.json`, `PolicyUpdateInput.json`)
    - `cargo test -p executor-core --test schema_snapshots` 통과 (env 없이 golden 일치 확인)
    - `EXPECTED_UNIMPL_CODE` 상수 값이 Task 2에서 확정된 wire code와 일치
    - `unimplemented_tools_return_phase_hint` 테스트가 (strategy_register→2, strategy_delete→2, strategy_run_once→6, policy_update→5) 매핑을 모두 assert
    - `readonly_tools_return_placeholder`가 strategy_list/policy_get/strategy_get/execution_get 네 tool을 모두 검증
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client ↔ executor-mcp (stdio) | Untrusted client sends arbitrary JSON-RPC — 스키마 검증 + unimplemented 반환이 유일한 방어선 |
| rmcp tool_router ↔ executor-core schema | `Parameters<T: JsonSchema>` wrapper가 입력 검증을 rmcp에 위임 |
| config.toml 파일 시스템 | deserialize 에러는 `anyhow::Context`로 래핑 — panic으로 stdout 오염 금지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | stdout stream (tracing 누출) | mitigate | `logging.rs`의 `with_writer(std::io::stderr)` (D-05a) + workspace clippy `print_stdout = "deny"` + 통합 테스트의 `recv()` JSON-RPC 파싱 assertion 3겹 |
| T-01-02-02 | Spoofing | 쓰기성 tool의 unimplemented 응답을 agent가 "성공"으로 오해 | mitigate | `ErrorCode(-32010)` (또는 fallback `-32603` + `data.code="unimplemented"`) + `data.tool` + `data.phase` 구조화 — 통합 테스트가 모두 assert |
| T-01-02-03 | Information Disclosure | 에러 message에 내부 경로/panic trace 포함 | mitigate | `anyhow::Context`로만 래핑 — stack trace는 RUST_BACKTRACE 설정시만 stderr로, stdout은 JSON-RPC error object만 |
| T-01-02-04 | Denial of Service | MCP client가 write tool을 무한 호출 → unimplemented 반복 | accept | Phase 1은 rate limiting 없음 — stdio 단일 클라이언트 모델이라 위협 낮음. Phase 5에서 재평가 |
| T-01-02-05 | Tampering | config.toml 필드 추가로 silent 오동작 | mitigate | `#[serde(deny_unknown_fields)]` — 모르는 필드는 deserialize 실패 |

</threat_model>

<verification>
- `cargo build -p executor-mcp` 성공
- `cargo clippy -p executor-mcp --all-targets -- -D warnings` 성공
- `cargo test -p executor-mcp --test stdio_handshake` 모두 통과 (tools_list_emits_full_surface + unimplemented_tools_return_phase_hint + readonly_tools_return_placeholder + harness_compiles)
- `cargo test -p executor-core --test schema_snapshots` 통과 (golden file 5개 존재)
- `grep -l "with_writer(std::io::stderr)" crates/executor-mcp/src/` 결과에 logging.rs 포함
- `grep -c "#\[tool(name" crates/executor-mcp/src/tools.rs` >= 8
</verification>

<success_criteria>
- MCP 클라이언트가 stdio로 `initialize` + `tools/list` + `tools/call` round-trip에 성공한다
- 8개 tool이 JSON Schema와 함께 노출된다 (`inputSchema` 필드 + `description` 필드 모두 존재)
- 쓰기성 tool 4개는 (primary) `ErrorCode(-32010)` 또는 (fallback) `McpError::internal_error`를 반환하며, 어느 쪽이든 `data.code="unimplemented"` + `data.phase` + `data.tool` + `data.hint` 구조 유지
- 읽기성 tool 4개는 placeholder 응답 (strategy_list=[], policy_get=empty object, strategy_get/execution_get=resource_not_found)
- stderr-only tracing + clippy lint로 stdout이 오염되지 않음 (integration test의 `recv()`가 JSON-RPC 파싱 assert)
- Config.toml 없이도 기본값으로 부팅 (D-06b)
</success_criteria>

<output>
완료 후 `.planning/phases/01-mcp-runtime-surface/01-02-SUMMARY.md` 생성. 포함 내용:
- ExecutorServer 구조와 tool_router/prompt_router 레이아웃
- **채택된 unimplemented wire code (-32010 또는 -32603) + PromptRouter 초기화 경로 (primary/fallback) 명시** — Plan 03가 Task 1에서 이 값에 맞춰 `impl ServerHandler` 블록을 확장한다
- 8개 tool의 phase 매핑 (strategy_register→2, policy_update→5, strategy_run_once→6, ...)
- unimplemented_err helper 시그니처 (Plan 03도 동일 helper를 쓸지, prompt는 placeholder 본문이 있으므로 안 쓸지 결정)
- config/logging 모듈의 공개 API
- 통합 테스트 3개의 정확한 함수 이름 (VALIDATION.md와 일치 확인)
</output>
</content>
</invoke>