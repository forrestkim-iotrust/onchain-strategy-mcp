---
phase: 07-examples-tests-and-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - README.md
  - README_ko.md
  - AGENTS.md
  - config.example.toml
autonomous: true
requirements:
  - VER-01
  - VER-02
  - VER-03
  - VER-04
  - VER-05
must_haves:
  truths:
    - "A reader can run the local Anvil examples from README instructions without guessing required config."
    - "A reader understands this is a local hot-wallet runtime and raw private keys must stay in env vars, not committed config."
    - "Agent-facing docs explain strategy authoring, review, policy, simulation, and execution report workflow."
    - "Docs list the safety regression commands covering policy, simulation, and sandbox boundaries."
  artifacts:
    - path: "README.md"
      provides: "English setup, example, safety, and verification instructions"
      contains: "Local Anvil examples"
    - path: "README_ko.md"
      provides: "Korean setup, example, safety, and verification instructions"
      contains: "로컬 Anvil 예제"
    - path: "AGENTS.md"
      provides: "Agent/operator workflow for strategy authoring and safe execution"
      contains: "execution_get"
    - path: "config.example.toml"
      provides: "Current config example including policy and signer sections without secrets"
      contains: "[signer]"
  key_links:
    - from: "README.md"
      to: "examples/strategies/erc20-approve.js"
      via: "documented local example command/path"
      pattern: "examples/strategies/erc20-approve.js"
    - from: "README.md"
      to: "crates/executor-mcp/tests/verification_safety.rs"
      via: "verification command list references safety suite"
      pattern: "verification_safety"
    - from: "AGENTS.md"
      to: "execution_get"
      via: "agent workflow tells agents to inspect receipt-backed reports"
      pattern: "execution_get"
---

<objective>
Refresh README, AGENTS, and config documentation so the completed runtime loop is discoverable and safe to operate.

Purpose: Phase 7 examples and tests only close the loop if readers and agents can run the examples, understand hot-wallet boundaries, and know which verification commands protect unsafe regressions.
Output: Updated English/Korean README content, AGENTS operator guidance, and current config example with no secrets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-local-managed-execution/06-01-SUMMARY.md
@.planning/phases/06-local-managed-execution/06-02-SUMMARY.md
@.planning/phases/06-local-managed-execution/06-03-SUMMARY.md
@.planning/phases/07-examples-tests-and-documentation/07-01-SUMMARY.md
@.planning/phases/07-examples-tests-and-documentation/07-02-SUMMARY.md
@README.md
@README_ko.md
@AGENTS.md
@config.example.toml
@examples/strategies/erc20-approve.js
@examples/strategies/generic-counter-call.js

<interfaces>
Executor must honor project GitNexus rules before editing symbols:
- This plan edits documentation/config examples only. If any code symbol is modified, run GitNexus impact analysis first.
- Run GitNexus detect changes before committing.

Documentation constraints:
- Do not document external signer adapters, detached execution, scheduler/reconcile loops, hosted custody, dashboard, TypeScript compiler, or multi-account management as v1 capabilities.
- State that v1 uses a local hot-wallet private key supplied only by env var reference: `[signer].private_key_env = "EXECUTOR_PRIVATE_KEY"`.
- Do not add raw private keys to committed docs/config.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README files with local examples and verification commands</name>
  <read_first>
    - README.md
    - README_ko.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/07-examples-tests-and-documentation/07-01-SUMMARY.md
    - .planning/phases/07-examples-tests-and-documentation/07-02-SUMMARY.md
    - examples/strategies/erc20-approve.js
    - examples/strategies/generic-counter-call.js
  </read_first>
  <files>README.md, README_ko.md</files>
  <action>
    Update `README.md` with sections named exactly `## What this runtime does`, `## Local hot-wallet safety model`, `## Local Anvil examples`, and `## Verification`. The examples section must reference `examples/strategies/erc20-approve.js`, `examples/strategies/generic-counter-call.js`, `examples/policies/local-anvil.toml`, `config.example.toml`, `strategy_register`, `strategy_run`, `execution_get`, and `execution://{run_id}`. The safety section must state that raw private keys belong only in the operator environment variable named by `[signer].private_key_env`, never in committed config or strategy JS. The verification section must list exact commands: `cargo test -p executor-mcp --features anvil-tests --test verification_examples -- --nocapture`, `cargo test -p executor-mcp --test verification_safety`, `cargo test --workspace`, and `cargo clippy --workspace --all-targets -- -D warnings`. Update `README_ko.md` with equivalent Korean sections and the same command/path literals. Do not claim unsupported v2/deferred capabilities.
  </action>
  <verify>
    <automated>grep -R "Local Anvil examples\|examples/strategies/erc20-approve.js\|verification_examples\|verification_safety" README.md README_ko.md && ! grep -R "external signer\|scheduler\|dashboard\|TypeScript compiler" README.md README_ko.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "## Local Anvil examples" README.md` returns exactly one match.
    - `grep -R "로컬 Anvil 예제" README_ko.md` returns at least one match.
    - `grep -R "examples/strategies/erc20-approve.js" README.md README_ko.md` returns at least two matches total.
    - `grep -R "cargo test -p executor-mcp --features anvil-tests --test verification_examples -- --nocapture" README.md README_ko.md` returns at least two matches total.
    - `grep -R "raw private key\|원문 개인키" README.md README_ko.md` returns at least one safety warning.
    - `grep -R "external signer\|scheduler\|dashboard\|TypeScript compiler" README.md README_ko.md | grep -v '^#'` returns no v1 capability claims.
  </acceptance_criteria>
  <done>README files explain the runtime loop, local hot-wallet safety model, example paths, and verification commands in English and Korean.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh AGENTS workflow and config example for current runtime</name>
  <read_first>
    - AGENTS.md
    - config.example.toml
    - .planning/phases/06-local-managed-execution/06-01-SUMMARY.md
    - .planning/phases/06-local-managed-execution/06-03-SUMMARY.md
    - .planning/phases/07-examples-tests-and-documentation/07-01-SUMMARY.md
    - .planning/phases/07-examples-tests-and-documentation/07-02-SUMMARY.md
  </read_first>
  <files>AGENTS.md, config.example.toml</files>
  <action>
    Update `AGENTS.md` with sections named exactly `## Strategy authoring loop`, `## Safety checks before execution`, `## Execution status and journal review`, and `## Commands agents should run`. The workflow must instruct agents to use `write_evm_strategy` / `review_evm_strategy` prompts where applicable, register with `strategy_register`, run with `strategy_run`, inspect `execution_get` or `execution://{run_id}`, and review journal resources for audit details. It must explicitly say agents must not request or print raw private keys and must not bypass policy/simulation failures. Update `config.example.toml` so comments no longer say `[policy]` and `[signer]` are future sections. Add current `[policy] path = "./policy.toml"` example comments and `[signer] private_key_env = "EXECUTOR_PRIVATE_KEY"` plus `receipt_timeout_ms = 120000`. Keep `config.example.toml` free of raw private-key values and dev default private keys.
  </action>
  <verify>
    <automated>grep -R "Strategy authoring loop\|execution_get\|\[signer\]\|private_key_env = \"EXECUTOR_PRIVATE_KEY\"" AGENTS.md config.example.toml && ! grep -R "0x59c6995e998f97a5a0044966f09453895e84e3b812dcc9a77b7b920c6f2e2c6d" AGENTS.md config.example.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "## Strategy authoring loop" AGENTS.md` returns exactly one match.
    - `grep -R "strategy_register\|strategy_run\|execution_get\|execution://{run_id}" AGENTS.md` returns matches for all four literals.
    - `grep -R "must not request or print raw private keys\|raw private keys" AGENTS.md` returns at least one match.
    - `grep -R "\[policy\]" config.example.toml` returns exactly one match.
    - `grep -R "\[signer\]" config.example.toml` returns exactly one match.
    - `grep -R "private_key_env = \"EXECUTOR_PRIVATE_KEY\"" config.example.toml` returns exactly one match.
    - `grep -R "0x59c6995e998f97a5a0044966f09453895e84e3b812dcc9a77b7b920c6f2e2c6d" AGENTS.md config.example.toml` returns no matches.
  </acceptance_criteria>
  <done>Agent workflow docs and config example match the shipped Phase 6/7 runtime and preserve the env-var-only signer boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → operator behavior | Docs influence how users configure signer and policy. |
| Agent guidance → strategy execution | AGENTS.md instructs AI agents how to operate runtime tools. |
| Example config → local runtime | Copied config can enable signing if env var is set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information Disclosure | README/AGENTS/config | mitigate | Explicitly prohibit raw private keys in committed config, docs, strategy JS, logs, or agent output; grep for known fixture private key. |
| T-07-03-02 | Elevation of Privilege | AGENTS workflow | mitigate | Document that agents must not bypass policy/simulation failures and must inspect execution reports after runs. |
| T-07-03-03 | Spoofing | local hot-wallet config | mitigate | State that signer address may appear in reports but private key must come only from operator-owned env var. |
| T-07-03-04 | Repudiation | verification docs | mitigate | List exact regression commands for examples, safety, workspace tests, and clippy. |
</threat_model>

<verification>
Run documentation and full checks:

```bash
grep -R "Local Anvil examples\|verification_examples\|verification_safety" README.md README_ko.md
grep -R "\[signer\]\|private_key_env = \"EXECUTOR_PRIVATE_KEY\"" config.example.toml
grep -R "strategy_register\|strategy_run\|execution_get" AGENTS.md
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
```
</verification>

<success_criteria>
- README.md and README_ko.md document the full runtime loop and exact local example verification commands.
- AGENTS.md tells agents how to author, review, run, and inspect strategies safely.
- config.example.toml includes current `[policy]` and `[signer]` sections without raw secrets.
- Docs do not present deferred v2 capabilities as shipped v1 features.
</success_criteria>

<output>
After completion, create `.planning/phases/07-examples-tests-and-documentation/07-03-SUMMARY.md`.
</output>
