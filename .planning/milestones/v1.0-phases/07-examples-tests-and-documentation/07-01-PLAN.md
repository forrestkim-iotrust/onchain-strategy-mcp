---
phase: 07-examples-tests-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/strategies/erc20-approve.js
  - examples/strategies/generic-counter-call.js
  - examples/policies/local-anvil.toml
  - crates/executor-mcp/tests/verification_examples.rs
autonomous: true
requirements:
  - VER-01
  - VER-02
must_haves:
  truths:
    - "A local Anvil run can execute an ERC20 approve strategy through the MCP runtime."
    - "A local Anvil run can execute a generic ABI contract call strategy through the MCP runtime."
    - "Example strategy files are real inputs consumed by tests, not disconnected snippets."
  artifacts:
    - path: "examples/strategies/erc20-approve.js"
      provides: "Runnable strategy source for ERC20 approve on local Anvil"
      contains: "ctx.actions.erc20Approve"
    - path: "examples/strategies/generic-counter-call.js"
      provides: "Runnable strategy source for generic ABI contract call on local Anvil"
      contains: "ctx.actions.contractCall"
    - path: "examples/policies/local-anvil.toml"
      provides: "Local policy allowing only documented Anvil example contracts/selectors"
      contains: "[chains]"
    - path: "crates/executor-mcp/tests/verification_examples.rs"
      provides: "Anvil-backed MCP integration proofs for VER-01 and VER-02"
      contains: "examples_erc20_approve_executes_on_anvil"
  key_links:
    - from: "crates/executor-mcp/tests/verification_examples.rs"
      to: "examples/strategies/erc20-approve.js"
      via: "test reads checked-in example strategy source"
      pattern: "include_str!.*erc20-approve.js"
    - from: "crates/executor-mcp/tests/verification_examples.rs"
      to: "examples/strategies/generic-counter-call.js"
      via: "test reads checked-in example strategy source"
      pattern: "include_str!.*generic-counter-call.js"
    - from: "crates/executor-mcp/tests/verification_examples.rs"
      to: "execution_get"
      via: "test confirms receipt-backed execution report after strategy_run"
      pattern: "execution_get"
---

<objective>
Create runnable local EVM examples and prove they execute through the full MCP runtime loop.

Purpose: Phase 7 must demonstrate that the strategy authoring surface, policy/simulation gates, local signer, broadcast, receipt persistence, and status query work together against a local EVM.
Output: Two checked-in JavaScript example strategies, one local Anvil policy fixture, and an Anvil-gated integration test file that consumes those exact examples.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-local-managed-execution/06-01-SUMMARY.md
@.planning/phases/06-local-managed-execution/06-02-SUMMARY.md
@.planning/phases/06-local-managed-execution/06-03-SUMMARY.md
@crates/executor-mcp/tests/stdio_handshake.rs
@crates/executor-evm/tests/common/anvil_fixture.rs
@crates/executor-evm/tests/fixtures/erc20.hex
@crates/executor-evm/tests/fixtures/counter.hex
@config.example.toml

<interfaces>
Executor must honor project GitNexus rules before editing symbols:
- Run GitNexus impact analysis before modifying any existing function/class/method/symbol. For this plan, creating new files is preferred; if an existing helper in `crates/executor-mcp/tests/common/mod.rs` or `crates/executor-mcp/tests/stdio_handshake.rs` must be edited, run impact analysis for that helper first.
- Report blast radius and stop before HIGH/CRITICAL edits.
- Run GitNexus detect changes before committing.

Existing test patterns available from `stdio_handshake.rs`:
```rust
use common::{call_tool, extract_json_result, spawn_server_with_config_text_and_env};
fn seed_strategy(db_path: &std::path::Path, name: &str, source: &str) -> Result<String>;
async fn spawn_server_with_policy_rpc_and_signer(...);
```

Existing local Anvil pattern available from `executor-evm` tests:
```rust
Anvil::new().chain_id(31337).try_spawn();
instance.endpoint_url();
instance.addresses();
```

Use the Anvil account private key only inside tests. Do not place raw private keys in committed TOML examples.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add checked-in local strategy and policy examples</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - crates/executor-mcp/tests/stdio_handshake.rs
    - config.example.toml
  </read_first>
  <files>examples/strategies/erc20-approve.js, examples/strategies/generic-counter-call.js, examples/policies/local-anvil.toml</files>
  <behavior>
    - VER-01: `erc20-approve.js` returns exactly one `ctx.actions.erc20Approve` action for a local ERC20 contract address supplied through the strategy input/context shape used by existing examples/tests.
    - VER-02: `generic-counter-call.js` returns exactly one `ctx.actions.contractCall` action with ABI text and function `increment` or equivalent state-changing counter method from the checked-in counter fixture.
    - Policy fixture allows chain `31337`, the documented example target contract placeholders, and only the selectors needed by the two examples.
    - No committed example file contains a raw private key.
  </behavior>
  <action>
    Create `examples/strategies/erc20-approve.js` as a plain JavaScript strategy function `(ctx) => [...]` that uses `ctx.actions.erc20Approve({ token, spender, amount })`. Use deterministic placeholder constants at the top named `TOKEN_ADDRESS`, `SPENDER_ADDRESS`, and `APPROVE_AMOUNT`, with comments telling tests/operators to replace the two addresses with local deployment addresses. Create `examples/strategies/generic-counter-call.js` as a plain JavaScript strategy function `(ctx) => [...]` that declares a minimal ABI array for a nonpayable counter method and calls `ctx.actions.contractCall({ address: COUNTER_ADDRESS, abi: JSON.stringify(COUNTER_ABI), function: "increment", args: [] })` or the exact function name present in the checked-in `counter.hex` fixture test usage. Create `examples/policies/local-anvil.toml` with `[chains] allow = [31337]`, `[contracts.31337] allow = [...]`, selector allow entries for ERC20 `approve(address,uint256)` selector `0x095ea7b3` and the counter call selector used by the generic example, `[native_value.31337] max_per_action = "0"`, and raw calls disabled. Do not add signer private keys to this policy or any example file.
  </action>
  <verify>
    <automated>test -f examples/strategies/erc20-approve.js && test -f examples/strategies/generic-counter-call.js && test -f examples/policies/local-anvil.toml && ! grep -R "0x59c6995e998f97a5a0044966f09453895e84e3b812dcc9a77b7b920c6f2e2c6d\|private_key\|PRIVATE_KEY" examples</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "ctx.actions.erc20Approve" examples/strategies/erc20-approve.js` returns at least one match.
    - `grep -R "ctx.actions.contractCall" examples/strategies/generic-counter-call.js` returns at least one match.
    - `grep -R "0x095ea7b3" examples/policies/local-anvil.toml` returns at least one match.
    - `grep -R "private_key\|PRIVATE_KEY" examples | grep -v '^#'` returns no matches.
  </acceptance_criteria>
  <done>Example files exist, are strategy-runtime-shaped, are policy-gated for local Anvil, and contain no raw signer secret.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove example strategies execute through MCP on Anvil</name>
  <read_first>
    - crates/executor-mcp/tests/stdio_handshake.rs
    - crates/executor-mcp/tests/common/mod.rs
    - crates/executor-evm/tests/common/anvil_fixture.rs
    - crates/executor-evm/tests/fixtures/erc20.hex
    - crates/executor-evm/tests/fixtures/counter.hex
    - examples/strategies/erc20-approve.js
    - examples/strategies/generic-counter-call.js
    - examples/policies/local-anvil.toml
  </read_first>
  <files>crates/executor-mcp/tests/verification_examples.rs</files>
  <behavior>
    - `examples_erc20_approve_executes_on_anvil` deploys/use a local ERC20 fixture, registers the checked-in `erc20-approve.js` source after replacing placeholder addresses, runs `strategy_run`, and verifies status `succeeded` plus an execution report with one confirmed action.
    - `examples_generic_contract_call_executes_on_anvil` deploys/use the checked-in counter fixture, registers the checked-in `generic-counter-call.js` source after replacing placeholder address, runs `strategy_run`, and verifies status `succeeded` plus an execution report with one confirmed action.
    - Tests are gated with `#[cfg(feature = "anvil-tests")]` and skip cleanly if Anvil cannot spawn.
    - Tests use `execution_get` after `strategy_run` to prove receipt-backed status, not only the immediate run response.
  </behavior>
  <action>
    Create `crates/executor-mcp/tests/verification_examples.rs` with `mod common;` and reuse existing stdio helpers (`initialize`, `call_tool`, `extract_json_result`, `spawn_server_with_config_text_and_env`). Under `#[cfg(feature = "anvil-tests")]`, add local helper code to spawn Anvil on chain 31337, deploy `crates/executor-evm/tests/fixtures/erc20.hex` and `counter.hex` with Alloy provider APIs, generate a temporary policy TOML allowing the deployed addresses/selectors, configure `[signer].private_key_env` through the process environment using an Anvil fixture key, register each example source into a temp SQLite DB, call `strategy_run`, then call `execution_get` with the returned run ID. Assert the report contains `status == "succeeded"`, exactly one action, `actions[0].status == "confirmed"`, non-empty `tx_hash`, non-empty `gas_used`, and no `error_kind`. If an existing common helper must be edited instead of duplicated, run GitNexus impact analysis for that helper before editing.
  </action>
  <verify>
    <automated>cargo test -p executor-mcp --features anvil-tests --test verification_examples -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "include_str!.*erc20-approve.js" crates/executor-mcp/tests/verification_examples.rs` returns at least one match.
    - `grep -R "include_str!.*generic-counter-call.js" crates/executor-mcp/tests/verification_examples.rs` returns at least one match.
    - `grep -R "examples_erc20_approve_executes_on_anvil" crates/executor-mcp/tests/verification_examples.rs` returns exactly one match.
    - `grep -R "examples_generic_contract_call_executes_on_anvil" crates/executor-mcp/tests/verification_examples.rs` returns exactly one match.
    - `cargo test -p executor-mcp --features anvil-tests --test verification_examples -- --nocapture` passes or skips only when Anvil is unavailable through the established skip-cleanly path.
  </acceptance_criteria>
  <done>VER-01 and VER-02 are proven by MCP-level Anvil integration tests that consume the checked-in example strategies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Example JS strategy → runtime | Checked-in example source becomes executable strategy code inside sandbox. |
| Test Anvil signer env → MCP process | Test-only private key enters child process environment. |
| Local policy fixture → signer handoff | Policy fixture determines which example actions can reach signing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | examples and policy fixture | mitigate | Keep raw private keys out of committed examples/policies; use env injection only inside tests and grep for secret markers. |
| T-07-01-02 | Tampering | example strategy placeholders | mitigate | Tests replace placeholders with freshly deployed local addresses and prove the checked-in source is the source under test. |
| T-07-01-03 | Elevation of Privilege | local policy fixture | mitigate | Allow only chain 31337 and the exact example contract selectors; raw calls remain disabled. |
| T-07-01-04 | Repudiation | example execution proof | mitigate | Verify `execution_get` receipt-backed action report after `strategy_run`, including tx hash and gas used. |
</threat_model>

<verification>
Run targeted and workspace checks:

```bash
test -f examples/strategies/erc20-approve.js && test -f examples/strategies/generic-counter-call.js && test -f examples/policies/local-anvil.toml
cargo test -p executor-mcp --features anvil-tests --test verification_examples -- --nocapture
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
```
</verification>

<success_criteria>
- Local ERC20 approve example exists and is executed by an Anvil-backed MCP test.
- Generic ABI contract call example exists and is executed by an Anvil-backed MCP test.
- Both example proofs confirm receipt-backed execution reports through `execution_get`.
- No raw private key is committed in examples or policy fixtures.
</success_criteria>

<output>
After completion, create `.planning/phases/07-examples-tests-and-documentation/07-01-SUMMARY.md`.
</output>
