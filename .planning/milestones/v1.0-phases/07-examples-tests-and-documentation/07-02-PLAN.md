---
phase: 07-examples-tests-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/executor-mcp/tests/verification_safety.rs
autonomous: true
requirements:
  - VER-03
  - VER-04
  - VER-05
must_haves:
  truths:
    - "Tests prove policy blocks disallowed chains before signing."
    - "Tests prove policy blocks disallowed contracts before signing."
    - "Tests prove policy blocks disallowed selectors before signing."
    - "Tests prove simulation failure prevents signing and broadcast."
    - "Tests prove the JavaScript sandbox blocks forbidden host access."
  artifacts:
    - path: "crates/executor-mcp/tests/verification_safety.rs"
      provides: "MCP-level safety regression suite for policy, simulation, and sandbox boundaries"
      contains: "policy_blocks_disallowed_chain_contract_and_selector_before_signing"
    - path: "crates/executor-mcp/tests/verification_safety.rs"
      provides: "Simulation failure before signer proof"
      contains: "simulation_failure_prevents_signing_and_records_no_tx_hash"
    - path: "crates/executor-mcp/tests/verification_safety.rs"
      provides: "MCP-level forbidden host access proof"
      contains: "sandbox_blocks_forbidden_host_access_through_strategy_run"
  key_links:
    - from: "crates/executor-mcp/tests/verification_safety.rs"
      to: "strategy_run"
      via: "MCP stdio tool calls exercise the production safety pipeline"
      pattern: "strategy_run"
    - from: "crates/executor-mcp/tests/verification_safety.rs"
      to: "execution_get"
      via: "post-failure report confirms no tx hash/signing occurred"
      pattern: "execution_get"
    - from: "crates/executor-mcp/tests/verification_safety.rs"
      to: "policy fixtures"
      via: "temporary TOML policies encode denied chain/contract/selector cases"
      pattern: "chain_not_allowed|contract_not_allowed|selector_not_allowed"
---

<objective>
Add a focused safety regression suite proving unsafe paths stop before signing or host access.

Purpose: The repo must prevent regressions that allow disallowed policy dimensions, failed simulations, or forbidden JavaScript host APIs to bypass the safety model.
Output: MCP-level verification tests covering VER-03, VER-04, and VER-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-simulation-and-policy-gate/05-05-SUMMARY.md
@.planning/phases/06-local-managed-execution/06-02-SUMMARY.md
@crates/executor-mcp/tests/stdio_handshake.rs
@crates/executor-policy/tests/eval_chains.rs
@crates/strategy-js/tests/sandbox_host_globals.rs
@crates/executor-evm/tests/fixtures/revert_counter.hex

<interfaces>
Executor must honor project GitNexus rules before editing symbols:
- Prefer creating the new integration test file only. If modifying existing helpers/functions, run GitNexus impact analysis for each existing symbol first.
- Report blast radius and stop before HIGH/CRITICAL edits.
- Run GitNexus detect changes before committing.

Existing safety semantics from prior phases:
- Policy denial categories include `chain_not_allowed`, `contract_not_allowed`, and `selector_not_allowed`.
- Simulation failure surfaces as `simulation_failure` / revert before signing.
- Sandbox host global tests currently assert absence of `console`, `fetch`, timer APIs, `XMLHttpRequest`, `WebSocket`, `process`, `Worker`, `child_process`, `fs`, `require("fs")`, `Deno`, and dynamic import success.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add policy-denial tests that prove no signing occurs</name>
  <read_first>
    - crates/executor-mcp/tests/stdio_handshake.rs
    - crates/executor-mcp/tests/common/mod.rs
    - crates/executor-policy/tests/eval_chains.rs
    - .planning/phases/05-simulation-and-policy-gate/05-05-SUMMARY.md
    - .planning/phases/06-local-managed-execution/06-02-SUMMARY.md
  </read_first>
  <files>crates/executor-mcp/tests/verification_safety.rs</files>
  <behavior>
    - VER-03: A strategy action denied by chain policy returns a policy-denied MCP error with `chain_not_allowed` and no execution row tx hash.
    - VER-03: A strategy action denied by contract policy returns a policy-denied MCP error with `contract_not_allowed` and no execution row tx hash.
    - VER-03: A strategy action denied by selector policy returns a policy-denied MCP error with `selector_not_allowed` and no execution row tx hash.
    - Each denial is tested through `strategy_run`, not only the `executor-policy` unit API.
  </behavior>
  <action>
    Create `crates/executor-mcp/tests/verification_safety.rs` with `mod common;` and helper functions mirroring `stdio_handshake.rs` patterns for temporary SQLite state, temporary policy TOML, strategy seeding, server spawning, `strategy_run`, and optional `execution_get`. Add one table-driven test named `policy_blocks_disallowed_chain_contract_and_selector_before_signing` with three cases: chain, contract, selector. For each case, register a strategy returning a single `ctx.actions.contractCall` or raw action whose normalized transaction would be signable if policy allowed it; configure `[signer]` with a test-only env var value so the test proves policy stops before signer use rather than failing from missing signer. Assert the JSON-RPC error/run status indicates `policy_denied`, the denial category in error data or journal decisions equals the expected category, and `StateStore::list_executions_for_run(run_id)` is empty or contains no `tx_hash`. Do not use production default private keys; env-inject the Anvil fixture key only into the child process.
  </action>
  <verify>
    <automated>cargo test -p executor-mcp --test verification_safety policy_blocks_disallowed_chain_contract_and_selector_before_signing</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "policy_blocks_disallowed_chain_contract_and_selector_before_signing" crates/executor-mcp/tests/verification_safety.rs` returns exactly one match.
    - `grep -R "chain_not_allowed" crates/executor-mcp/tests/verification_safety.rs` returns at least one match.
    - `grep -R "contract_not_allowed" crates/executor-mcp/tests/verification_safety.rs` returns at least one match.
    - `grep -R "selector_not_allowed" crates/executor-mcp/tests/verification_safety.rs` returns at least one match.
    - `grep -R "list_executions_for_run" crates/executor-mcp/tests/verification_safety.rs` returns at least one match.
    - `cargo test -p executor-mcp --test verification_safety policy_blocks_disallowed_chain_contract_and_selector_before_signing` passes.
  </acceptance_criteria>
  <done>Policy regressions for chains, contracts, and selectors are MCP-visible and proven to stop before signing/broadcast.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add simulation-failure and sandbox-host regression tests</name>
  <read_first>
    - crates/executor-mcp/tests/stdio_handshake.rs
    - crates/executor-mcp/tests/common/mod.rs
    - crates/strategy-js/tests/sandbox_host_globals.rs
    - crates/executor-evm/tests/fixtures/revert_counter.hex
    - .planning/phases/05-simulation-and-policy-gate/05-05-SUMMARY.md
  </read_first>
  <files>crates/executor-mcp/tests/verification_safety.rs</files>
  <behavior>
    - VER-04: A strategy calling a reverting local contract fails at simulation and does not produce a signed/broadcast execution row with tx hash.
    - VER-05: A strategy attempting forbidden host access through `process`, `fetch`, `require("fs")`, or dynamic import is rejected or returns the expected no-access result through `strategy_run`.
    - Tests are MCP-level where possible so they protect the integrated runtime path, while preserving existing `strategy-js` unit coverage.
  </behavior>
  <action>
    Extend `verification_safety.rs` with `simulation_failure_prevents_signing_and_records_no_tx_hash` under `#[cfg(feature = "anvil-tests")]`. Spawn Anvil, deploy `revert_counter.hex`, allow the deployed contract/selector in policy, configure a signer env var, run a strategy that calls the reverting function, assert the MCP error code/data indicates simulation failure/revert, and assert `StateStore::list_executions_for_run(run_id)` is empty or has no `tx_hash`. Add `sandbox_blocks_forbidden_host_access_through_strategy_run` without an Anvil feature gate: register a strategy that checks `typeof globalThis.process`, `typeof fetch`, guarded `require("fs")`, and dynamic import behavior; the successful safe outcome must be `noop`, and any reachable forbidden global must return an invalid output marker that makes the test fail. Keep `eval` and `Function` out of the forbidden list because prior Phase 3 tests intentionally allow them as sandboxed intrinsics. If dynamic import returns a Promise and triggers invalid output, accept that as a blocked-host outcome only when no module load succeeds.
  </action>
  <verify>
    <automated>cargo test -p executor-mcp --test verification_safety sandbox_blocks_forbidden_host_access_through_strategy_run && cargo test -p executor-mcp --features anvil-tests --test verification_safety simulation_failure_prevents_signing_and_records_no_tx_hash -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "simulation_failure_prevents_signing_and_records_no_tx_hash" crates/executor-mcp/tests/verification_safety.rs` returns exactly one match.
    - `grep -R "sandbox_blocks_forbidden_host_access_through_strategy_run" crates/executor-mcp/tests/verification_safety.rs` returns exactly one match.
    - `grep -R "process\|fetch\|require(\"fs\")\|import(" crates/executor-mcp/tests/verification_safety.rs` returns matches in the sandbox test source.
    - `grep -R "list_executions_for_run" crates/executor-mcp/tests/verification_safety.rs` returns at least two matches across policy/simulation checks.
    - `cargo test -p executor-mcp --test verification_safety sandbox_blocks_forbidden_host_access_through_strategy_run` passes.
    - `cargo test -p executor-mcp --features anvil-tests --test verification_safety simulation_failure_prevents_signing_and_records_no_tx_hash -- --nocapture` passes or skips only when Anvil is unavailable through the established skip-cleanly path.
  </acceptance_criteria>
  <done>Simulation failure and forbidden host access are covered by integrated regression tests that prevent unsafe signing and sandbox regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted strategy JS → sandbox/runtime | Strategy source attempts host API access or unsafe actions. |
| Policy TOML → execution gate | Test policy controls whether actions reach simulation/signing. |
| Simulation result → signer boundary | Reverts must stop before local signer/broadcast. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Elevation of Privilege | policy gate regression tests | mitigate | MCP-level tests assert chain/contract/selector denials and no tx hash/execution broadcast rows. |
| T-07-02-02 | Tampering | simulation gate regression test | mitigate | Reverting Anvil contract must produce simulation failure before signer use. |
| T-07-02-03 | Information Disclosure | sandbox host access test | mitigate | Strategy-run test asserts forbidden host globals/modules are inaccessible. |
| T-07-02-04 | Repudiation | failure audit checks | mitigate | Tests inspect persisted run/execution state so failures are observable and not silently skipped. |
</threat_model>

<verification>
Run targeted and workspace checks:

```bash
cargo test -p executor-mcp --test verification_safety policy_blocks_disallowed_chain_contract_and_selector_before_signing
cargo test -p executor-mcp --test verification_safety sandbox_blocks_forbidden_host_access_through_strategy_run
cargo test -p executor-mcp --features anvil-tests --test verification_safety simulation_failure_prevents_signing_and_records_no_tx_hash -- --nocapture
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
```
</verification>

<success_criteria>
- Policy denial tests cover chain, contract, and selector dimensions through MCP `strategy_run`.
- Simulation failure test proves no signing/broadcast tx hash is produced after a revert.
- Sandbox host access test proves forbidden APIs remain blocked through the integrated runtime path.
- All new tests are automated and suitable for regression runs.
</success_criteria>

<output>
After completion, create `.planning/phases/07-examples-tests-and-documentation/07-02-SUMMARY.md`.
</output>
