{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "StrategyRegisterInput",
  "description": "Register a JavaScript strategy (content-addressed; idempotent on same source).",
  "type": "object",
  "properties": {
    "description": {
      "description": "Optional free-form description (max 4096 chars).",
      "type": [
        "string",
        "null"
      ]
    },
    "name": {
      "description": "Human-readable name; UNIQUE among non-deleted strategies.",
      "type": "string"
    },
    "source": {
      "description": "JavaScript source — executed in a sandbox starting Phase 3. Max 256 KiB.",
      "type": "string"
    },
    "tags": {
      "description": "Optional tags (max 16 items, each max 64 chars).",
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "string"
      }
    }
  },
  "additionalProperties": false,
  "required": [
    "name",
    "source"
  ]
}
