{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ExecutionIdInput",
  "description": "Input for execution_get; identifies a run by its run ID.",
  "type": "object",
  "properties": {
    "run_id": {
      "description": "Run ID returned from a previous `strategy_run`.",
      "type": "string"
    }
  },
  "required": [
    "run_id"
  ]
}
