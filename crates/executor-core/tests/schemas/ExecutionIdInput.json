{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ExecutionIdInput",
  "description": "Input for execution_get; the legacy field name accepts a run ID.",
  "type": "object",
  "properties": {
    "execution_id": {
      "description": "Run ID returned from a previous `strategy_run`.",
      "type": "string"
    }
  },
  "required": [
    "execution_id"
  ]
}
