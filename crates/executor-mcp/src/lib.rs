#![deny(clippy::print_stdout, clippy::print_stderr, clippy::dbg_macro)]
//! `executor-mcp` — stdio MCP server entry. Phase 1 Plan 01 leaves this crate
//! as a skeleton; Plan 02 wires in tool/prompt routers, Plan 03 adds resources
//! and the stdout-discipline integration tests.

/// Placeholder so downstream plans can add modules without moving this file.
pub fn placeholder() {}
